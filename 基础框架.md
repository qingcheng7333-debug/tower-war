# 嘗試ゲーム · 游戏开发骨架文档 v4.7

> **定位**：本文档记录游戏的**架构设计、模块划分、数据流和接口约定**（骨架级规范）。
> **本文档不记录具体数值**——出现单位/卡牌名仅作机制举例，其数值一律以 `游戏内容.txt` 为准。
> 更新日志请移步 `更新日志.txt`。

---

## 一、游戏概述

- **类型**：2D横屏双人实时塔防对战（本地AI / LLM-AI / 本机双人 / 未来联机）
- **核心玩法**：消耗圣水部署兵种/建筑/法术，摧毁对方主塔获胜
- **技术栈**：纯 HTML5 Canvas + JavaScript（多文件拆分，全局挂载）
- **目标平台**：桌面端 Chrome 浏览器，支持鼠标操作
- **时间驱动**：逻辑帧（Tick）固定 **30 次/秒**（`FIXED_DELTA = 1/30`）；渲染层 `requestAnimationFrame` + 插值绘制
  - 主页「设置」区帧率三档（不限制/60/30）**仅限制渲染帧率**，与逻辑 tick 解耦（逻辑恒 30Hz），localStorage 记忆
  - 单帧累积上限 0.1s + `MAX_STEPS_PER_FRAME=5` 双保险，防螺旋死亡与追帧爆炸

---

## 二、全局数据分类

为避免各文件对"能不能碰全局变量"的规则打架，明确区分两种全局数据：

| 分类 | 特性 | 谁能读 | 谁能写 |
|------|------|--------|--------|
| **全局常量数据**(config-like) | 游戏启动后**永不改变**，等价于写死的参数 | 任何文件 | 仅 `config.js` 定义一次 |
| **全局可变状态**(game-like) | 运行时持续变化 | 任何文件可读 | 仅 `entities.js`(瞬时操作)与 `update.js`(帧驱动)可写 |

> 举例：`CARDS`, `W`, `H` 属于常量数据；`game` 对象（圣水、实体数组等）属于可变状态。

**工具函数约束**：`utils.js` 的纯函数规则特指**不得读写 `game` 这一全局可变状态对象**，但可以读取全局常量数据。

---

## 三、项目文件结构

```text
未完成的ギャルゲ/
├── index.html              # 入口页面（主页 + 游戏容器 + 弹窗）
├── css/
│   └── style.css           # 全局样式
├── js/
│   ├── config.js           # 常量、卡牌模板、地图尺寸、全局参数
│   ├── gameState.js        # 运行时状态 game 对象定义、resetGame()
│   ├── utils.js            # 纯工具函数（不读写 game）
│   ├── entities.js         # 实体创建 + 部署入口 + 法术执行
│   ├── ai测试组.js          # 内置AI行为组1：经典策略（gatherIntel/chooseCard/pickPosition）
│   ├── ai哥布林组.js         # 内置AI行为组2（空壳示例，接口同 ai测试组.js）
│   ├── aiSelector.js        # AI行为组注册表 + 人机选择管理（选择/新建/刷新/决策调度）
│   ├── apiAI.js            # LLM-AI 通道 + 预设/配置管理
│   ├── network.js          # 联机网络层（VibeHub Room + Lockstep 延迟缓冲同步，单机零影响）
│   ├── update.js           # 每帧更新逻辑（状态推进、特效、清理）
│   ├── render.js           # 所有 Canvas 绘制函数
│   ├── ui.js               # 玩家交互事件 + 卡牌面板渲染
│   ├── deckManager.js      # 卡组管理系统（localStorage 持久化）
│   └── main.js             # 程序入口、页面切换、游戏循环、人机选择弹窗
├── 個人データ.js            # 用户数据文件（可选，含 API Key；导出下载/导入恢复，分享前必删）
├── 基础框架.md              # 骨架规范（本文档）
├── 联机架构.md              # 联机子系统设计文档（协议/状态机/同步模型，v11.64）
├── 游戏内容.txt              # 具体数值、卡牌详情、机制说明
├── 更新日志.txt              # 版本变更记录
└── 游戏日志.txt              # 开发/测试日志
```

---

## 四、文件职责（骨架级接口约定）

### 4.1 config.js

**职责**：存放所有静态数据，**不包含任何函数或可变状态**，纯数据文件。

**包含内容**：
- 地图尺寸常量 `W, H, BUFFER_WIDTH, RIVER_LEFT, RIVER_RIGHT`
- 逻辑帧常量 `TICK_RATE`(30) / `FIXED_DELTA`(1/30 秒) / `TICK_INTERVAL_MS` / `MAX_STEPS_PER_FRAME`(5，防螺旋死亡)
- 阵营坐标（主塔、堡垒等）
- 阵营标识常量 `TEAMS = ['player', 'ai']`（联机前置：对称阵营索引；单机时 `'ai'` 键交给本地 AI 托管；`opponentTeam()` 见 4.3）
- **卡牌模板** `CARDS` 对象：每张卡定义：
  - `type`：可选值 `'troop' | 'tower' | 'barrack' | 'collector' | 'spell' | 'healer'`
    > 注意：`main_tower` / `bastion` 是**内置建筑实体类型**，不出现在 CARDS 中，由 `gameState.js` 开局直接创建
  - `name`, `cost`, `hp`, `atk`, `atkSpeed`, `moveSpeed`, `range`, `targetMode` 等基础属性
  - `category`：可选 **UI 分类标记**（当前 `'elite'`=精锐，卡牌面板显示金色；**不参与游戏逻辑**，type 仍按 `'troop'` 判定，AI/部署/战斗逻辑不受影响）
  - 特殊字段（骨架级登记，见下表）
  - 展示用：`icon` (emoji)
- 卡牌ID列表 `CARD_IDS = Object.keys(CARDS)`
- 统一定义的子模板常量（如 `BASE_UNITS` 生产模板、`BASTION_STATS` 堡垒模板）

**卡牌特殊字段一览**（骨架级；具体数值以 `游戏内容.txt` 为准）：

| 字段 | 作用 |
|------|------|
| `flying` / `onlyGround` / `groundOnly` | 飞行单位 / 只打地面 / 不对空 |
| `splash` | 溅射半径 |
| `pierce` | 穿透攻击（弹道穿过多个目标） |
| `deployDelay` / `cooldown` | 部署延迟时间 / 再次使用冷却 |
| `count` | 一次部署的实体数量（群体单位） |
| `anywhere` | 任意位置部署（豁免半场/河流限制，如矿工类） |
| `towerDmgMul` | 对防御工事（主塔/堡垒）的伤害系数 |
| `spawnUnit` / `spawnInterval` / `spawnCount` | 周期性召唤（兵营、女巫类） |
| `deathSpawnCount` | 死亡时召唤的单位数量 |
| `deathBoomRadius` / `deathBoomDmg` | 死亡时范围爆炸（通用死亡爆炸机制） |
| `deploySpell` | 部署瞬间触发的一次性法术（范围伤害/眩晕/落雷/击退等） |
| `chainRange` / `chainCount` / `chainDmgMul` | 连锁闪电参数 |
| `healAmount` / `healSpeed` | 治疗参数（治疗兵/治疗脉冲类） |
| `zoneDuration` / `rageBoost` / `boostDuration` / `rageTick` | 狂暴法术区域参数：区域持续时长 / 增益系数 / 单次buff时长 / 脉冲间隔（区域内每 tick 对友方施加一次buff） |
| `reflectCooldown` / `reflectMultiplier` | 近战反弹参数（浪人类）：格挡近战伤害并反弹的冷却 / 反弹倍率 |
| `chargeTime` | 蓄能类（电磁炮等）蓄能所需时长 |
| `strikes` | 多段延迟法术（箭雨/地震/大雷电）的段数 |
| `leapRange` | 跳跃类（超级骑士等）跳跃落点距离 |
| `attackHeal` / `deployHeal` / `healTicks` / `healInterval` / `healDuration` | 治疗脉冲类（战斗天使等）：攻击触发的每次治疗量 / 登场治疗量 / 总次数 / 间隔 / 持续窗口 |
| `healRadius` / `healRate` | 治疗范围半径 / 每秒治疗速率（持续光环类） |
| `hookMin` | 渔夫钩子最小作用距离 |
| `shield` | 初始护盾值（实体第二血条，所有实体兜底存在、无盾为 0；机制详见 5.10） |

**规则**：
- 新增卡牌只需在此文件追加属性，`CARD_IDS` 自动包含
- 其他文件通过 `CARDS[id]` 只读访问，严禁直接修改

### 4.2 gameState.js

**职责**：定义并管理运行时可变状态 `game` 对象。

**核心导出**：
- `game` — 全局游戏状态对象
- `entityIdCounter` — 实体唯一 ID 自增计数器
- `resetGame(seed?)` — 重置所有状态为初始值（同时创建双方主塔与堡垒实体）；**`seed` 为可选随机种子**（联机前置：开局同步种子，两端 `rand()` 序列完全一致；单机默认用时间戳）
- `getBattleStateSnapshot()` — 导出**可 JSON 序列化的纯净战场快照**（联机前置：状态同步/调试/观战用；只含核心战斗数据，**不含 `uiState` 本地 UI 状态**）

**game 对象骨架字段**（选列核心类别，非完整列表）：
| 类别 | 字段举例 | 说明 |
|------|---------|------|
| 圣水 | `elixir{player,ai}`, `maxElixir`, `baseElixirRate`, `elixirMultiplier{player,ai}` | 双方圣水管理 + 丢堡帮扶倍率（**阵营对称字典**：按 team 键索引，联机前置） |
| 实体 | `entities[]` | 所有动态对象的容器 |
| 胜负 | `gameOver`, `winner` | 对局结束标志 |
| 交互（本地UI） | `uiState{ selectedCardId, selectedCardId2, deployFailReason, mouseX, mouseY, hoveredEntity, _lastClick }` | **纯本地交互状态**（联机前置：与核心战斗状态分离，不参与同步/快照）；`*2` 字段供双人模式上方（红方）玩家使用 |
| 镜像追踪 | `lastDeployedCardId`, `lastDeployedCardId2` | 双方上一次部署的卡牌（**镜像法术依赖，属战斗数据**，保留顶层不收纳） |
| AI | `aiDecisionTimer`, `aiThinking`, `gameMode` | AI 调度状态；`gameMode`: `'classic'` / `'deck'` / `'api'` / `'local_multi'`（双人） / `'online'`（联机） |
| 特效 | `projectiles[]`, `spellEffects[]`, `dmgNumbers[]`, `lightningChains[]`, `deployLightnings[]`, `deployEffects[]`, `pierceArrows[]`, `speedZones[]`, `rageZones[]`, `freezeZones[]`, `arrowRainStrikes[]`, `fireballStrikes[]`, `earthquakeStrikes[]`, `thunderStrikes[]`, `bombs[]`, `fishingLines[]` | 各类特效数据队列（详见 5.4） |
| 部署 | `deploying[]` | 部署延迟队列 |
| 冷却 | `cardCooldowns{ player:{}, ai:{} }` | 卡牌冷却追踪 |
| 堡垒 | `bastionsLost{player,ai}`, `lastBastionPromptLevel` | 已丢堡垒数（**阵营对称字典**）、帮扶提示等级 |
| 时间 | `time` / `tick` | 游戏已运行时间(秒) / 当前逻辑帧号（Fixed Timestep 联机同步锚点） |

> **主塔/堡垒血量**：不设独立字段——主塔与堡垒就是 `entities[]` 中的实体（`type:'main_tower'` / `type:'bastion'`，带 `fortification` 标记），血量存于实体 `hp/maxHp`。

**修改权限规则**（核心框架约束）：
- **帧驱动修改**（每帧持续逻辑：移动、攻击、生产等）→ 只能在 `update.js` 中修改 `game`
- **事件驱动修改**（部署、法术触发等瞬时操作）→ 允许在 `entities.js` 中修改 `game`
- 两种修改路径互不交叉，清晰分离

> **规则**：每局开始前必须调用 `resetGame()`。

### 4.3 utils.js

**职责**：纯工具函数包，不读写 `game` 全局可变状态。

**核心函数**：
- `dist(a, b)` — 计算两点距离
- `isInHalf(x, y, isPlayer, aiBastionsLost, playerBastionsLost)` — 检查坐标是否在己方可部署半场；后两个参数是**双方已丢堡垒数**，决定可部署区边界是否扩展（丢堡≥1 可越河、≥2 可推进到敌方堡垒线）
- `canDeployHere(cardId, team, x, y, entities?)` — 检查部署位置合法性（**不检查费用**！）：
  - `spell` 法术与带 `anywhere` 标记的卡不受半场/河流限制
  - 建筑类（tower/barrack/collector/bastion/main_tower）额外检查**不与已有建筑/堡垒/主塔重叠**
  - 注意：`entities` 参数由调用方传入，不直接读 `game.entities`
- `getEntityHalfSize(e)` — 实体碰撞半宽（供建筑部署重叠检测）
- `rand()` / `setRandomSeed(seed)` / `getRandomSeed()` — **确定性 PRNG（Mulberry32）**：逻辑层所有随机一律走 `rand()`（已替换 update.js / entities.js 的全部 `Math.random()`）；同种子序列完全一致，是联机确定性的核心
- `opponentTeam(team)` — 返回对方阵营键（`'player'`↔`'ai'`；配合 `TEAMS` 常量，联机前置阵营对称化）
- `worldToScreen(x, y, isFlipped)` / `screenToWorld(sx, sy, isFlipped)` — **世界/屏幕坐标翻转抽象**（联机前置：逻辑层恒用世界坐标，渲染层未来按视角做上下镜像适配；`isFlipped=true` 时 `y → H - y`，x 不翻转）

**规则**：
- 必须为绝对的纯函数，无副作用，可安全复用
- 不修改任何传入的参数
- 可以读取全局常量数据（如 `CARDS`, `W`, `H`, `RIVER_LEFT`, `PLAYER_TOWER`, `BASE_UNITS` 等），因为常量等价于函数参数中的静态配置
- **不得直接读写 `game` 全局可变状态对象**
- 所有需要判断的外部可变数据必须通过参数传入，不能在函数内部自己去取 `game.entities`

> **PRNG 例外登记**：`rand()` / `setRandomSeed()` / `getRandomSeed()` 是本文件**唯一的有状态工具**——状态为模块级种子变量（非 `game` 对象），不违反"不得读写 `game`"约束；与 update.js 圣水 DOM 特例对等登记。渲染层（render.js）的视觉抖动**保留 `Math.random()`**（纯绘制效果，不参与逻辑确定性，避免污染种子流）。

> **`canDeployHere` 特别说明**：它**只判断"位置是否合法"**（半场/边界/河流/建筑重叠），**不判断"圣水是否够用"**——费用检查是 `deploy()` 自己的职责。两者不交叉重复实现。

### 4.4 entities.js

**职责**：实体生成、部署执行、法术伤害等瞬时操作。

**核心函数**：
- `createEntity(base)` — 合并默认属性，分配唯一 `id`，返回新实体对象
- `deploy(cardId, team, x, y)` — **部署入口**（玩家、AI、双人共用）：
  1. 检查圣水是否足够
  2. 检查冷却是否结束
  3. 检查位置是否合法（调用 `canDeployHere`）
  4. 扣除圣水 + 设置冷却
  5. 将部署项加入 `game.deploying[]` 延迟队列（不立即生成）
- `finishDeployItem(item)` — 延迟队列到期后，实际创建实体或执行法术
- `applySpellDamage(card, casterTeam, x, y)` — 法术执行入口（标记死亡但不移除）
- `createXxx(x, y, team)` — 各召唤物/子单位的独立创建函数（如蝙蝠、骷髅、幼崽、送水人等）

**特殊兵种初始化**（`finishDeployItem` 部署时附加标记字段；单位名仅作机制举例）：
| 标记字段 | 所属单位类型 | 作用 |
|---------|------------|------|
| `spawnTimer` | 女巫类 | 周期性召唤计时 |
| `isSiege` | 攻城人类 | 触碰建筑自爆、死亡留下💣 |
| `_iceBean` / `_fireBean` | 冰豆 / 火豆 | 自爆类单位标记 |
| `_stealthed` + `_stealthTimer` | 幽灵类 / 突袭类 | 初始隐身；突袭类蓄力时短暂隐身 |
| `towerDmgMul` | 矿工类 | 对防御工事伤害系数 |
| `_chargeTimer` + `_charging` | 冲锋类 | 冲锋倒计时（未触发为 false） |
| `_chargeTimer` + `_chargeMax` | 蓄能类（电磁炮等） | 蓄能进度 |
| `_leapCharging` / `_leapTimer` | 跳跃类（超级骑士等） | 蓄力跳跃状态 |
| `_assaultCharging` / `_assaultTimer` | 突袭类（暗影刺客等） | 突袭冲刺状态 |
| `_souls` / `_level` | 灵魂升级类（冥王等） | 击杀升级计数 |
| `_soulsPerLevel` / `_maxLevel` / `_baseHp` / `_baseAtk` | 灵魂升级类 | 升级所需灵魂数 / 最大等级 / 基础血攻（升级按基础值成长） |
| `_reflectTimer` | 浪人 | 近战反弹冷却（初始 0，触发时置为卡牌 `reflectCooldown`） |
| `_leapTargetId` | 跳跃类 | 蓄力跳跃期间锁定目标 |
| `_assaultTargetId` | 突袭类 | 突袭冲刺期间锁定目标 |
| `_healActive` / `_healTicks` / `_healTickTimer` / `_healAmount` | 治疗脉冲类（战斗天使） | 登场/攻击触发的持续治疗脉冲（窗口/剩余次数/间隔/单次量） |
| `_eggPulseTimer` / `_hasRegen` | 蛋/自回类（战斗天使） | 孵化脉冲计时 / 常驻自回buff（❤️‍🩹） |
| `_beamTimer` / `_beamTargetId` / `_beamSwitchCooldown` | 光束类（地狱塔） | 光束灼烧计时 / 锁定目标 / 切换目标冷却 |
| `deploySpell` 触发 | 部署带法术单位 | 部署瞬间的范围伤害/眩晕/落雷/击退特效 |

**两段式部署特例**（矿工类）：`finishDeployItem` 内部重新入队实现——部署延迟结束后先生成**土堆特效**（纯特效，无实体、不参与碰撞），再等挖地潜伏时间后生成真正实体破土出现。

**applySpellDamage 法术分支**（骨架级）：
- 区域类：极速加速区域、狂暴狂暴区域、冰冻冰封区域
- 召唤类：蝙蝠召唤
- 控制类：飓风拉拢
- 多段延迟结算类：箭雨、地震、大雷电、火球（各自入对应延迟队列）
- 复制类：复制范围内友军兵种（复制体带 hp=1 标记，禁止二次复制）
- 兜底：其他未特判的伤害法术走通用立即结算

**规则**：
- 所有单位必须通过 `createEntity` 创建
- **费用检查归 `deploy` 自己，位置检查必须调用 `canDeployHere`**，两者不交叉重复实现
- 法术只标记 `hp <= 0`，不做数组移除（移除归 `update.js`）
- 防御工事（`fortification`：主塔/堡垒）受法术伤害时应用 `towerDmgMul` 系数

**主塔血量修改权限写死**：`entities[]` 中主塔实体（`type:'main_tower'`）的 `hp` 只能被以下两类操作修改：
1. `entities.js` 的 `applySpellDamage`（法术类瞬时伤害命中主塔时）
2. `update.js` 的攻击逻辑（`attackTroop` 等，单位/建筑攻击命中主塔时）
其他任何文件（`ui.js` / `aiSelector.js` / `utils.js` / `render.js`）**禁止直接改动主塔血量**。

### 4.5 aiSelector.js（含内置 AI 行为组）

**职责**：经典模式（`gameMode='classic'` / `'deck'`）的本地 AI 决策调度 + 行为组注册表 + 人机选择管理。

**行为组文件规范**（`js/aiXxx.js`）：
- 每个行为组 = 一个独立文件，定义 `window.AIGroupXxx = { name, file, makeDecision }`
  - `makeDecision()` — 组内自主决策（`ai测试组.js` 内含经典策略：`gatherIntel()` 采集局势 → `chooseCard()` 选卡（即时反制+战略模式）→ `pickPosition()` 定坐标 → 调 `entities.js` 的 `deploy()` 部署）
- **自定义组**（新建组下载的模板）必须在文件内调用 `registerAIGroup(id, module)` **自注册**
- **内置组**（ai测试组/ai哥布林组）由本文件静态挂载，且用 `typeof` **防御式检查**：文件被删/缺失 → 该组不出现在列表，**其余全部正常**（不崩）

**核心数据**：
- `AI_GROUPS` — 行为组注册表 `{ id: { module } }`（内置组静态挂载 + 自定义组 `registerAIGroup` 自注册）
- `AI_BUILTIN_FILES` — 内置组文件名映射 `{ test: 'ai测试组.js', goblin: 'ai哥布林组.js' }`（「刷新」据此探测）
- `AI_CUSTOM_GROUPS_KEY` — 自定义组记录 localStorage 键（`towerwar_ai_custom_groups`）

**核心函数**：
- `registerAIGroup(id, module)` — 自定义组自注册（由行为组文件加载时调用）
- `getCustomGroupRecords()` / `addCustomGroupRecord(rec)` — 自定义组记录（localStorage 持久化，含 name/file）
- `loadCustomGroupScripts()` — 页面加载时按记录注入尚未注册的自定义组文件（带时间戳防缓存）
- `buildAiGroupTemplate(name, id, file)` — 「新建组」模板生成（下载 `aiXxx.js`）
- `renderAiGroupList()` — 渲染人机选择列表（内置组 + 自定义组；已记录未注册显示灰色占位）
- `getSelectedAIGroup()` / `setSelectedAIGroup(gid)` — 当前选中行为组（localStorage）
- `aiMakeDecision()` — **被 `update.js` 每 2.0 秒调用一次** → 转发到当前选中行为组的 `makeDecision()`；选中组缺失时**回退第一个可用组**，全缺失则安全返回

**人机选择弹窗**（main.js 绑定）：
- 列表 = 内置组 + 已注册自定义组（可选） + 已记录未注册（灰色占位）
- 「➕ 新建组」— 输入名字 → 下载模板 `aiXxx.js` → 放入 `js/` 目录；禁止与内置组同名（防覆盖）
- 「🔄 刷新」— **全量重新探测**（内置 + 全部自定义记录）：逐个重新注入文件，能加载并自注册的保留为选项；文件缺失/语法错误/未自注册的**自动移除**（自定义组同时从配置记录删除；内置组直接从注册表移除）；带防重入锁
- 「✖ 关闭」— 关闭弹窗

**AI 调用约束**（见基础框架不可违背原则）：
- `aiMakeDecision()` **不允许注册自己的定时器/事件监听**
- 唯一触发点是 `update.js` 的帧驱动计时器判断
- AI 必须通过 `deploy()` 部署，不许绕过

> 具体AI策略（即时反制规则、战略模式切换条件、位置规则等）详见 `游戏内容.txt`。

### 4.6 apiAI.js

**职责**：LLM 模式（`gameMode='api'`）的 AI 通道 + 预设配置管理。

**核心函数**：
- `llmAiMakeDecision()` — 被 `update.js` 每 2.5 秒调用一次
- `callLlmDecision()` — 请求封装（超时 8 秒，构建战场摘要 + System Prompt，调用 OpenAI 兼容 API）
- `parseDeploymentText()` — 解析 LLM 返回的部署指令（纯文本格式）
- `buildLlmBattleState()` / `buildCardDescriptions()` / `getSystemPrompt()` — 战场摘要构建
- `fallbackDeployNear(cardId, baseX, baseY)` — LLM 坐标不合法时的**就近偏移修正**
- `buildRequestConfig()` — 请求地址构建（支持 Cloudflare Worker 代理转发）
- `getLlmConfig()`
- `testLlmConnection(apiKey, baseUrl, model, proxyUrl)` — 测试连接（返回成功/失败+延迟）

**预设管理函数**：
- `getPresets()`, `setPresets()`, `savePreset()`, `deletePreset()`
- `getActivePresetId()`, `setActivePreset(id)`, `applyPreset(preset)`

**生命周期**：
1. `update.js` 判断 `game.aiDecisionTimer >= 2.5 && !game.aiThinking`
2. 调用 `llmAiMakeDecision()` → `game.aiThinking = true`
3. 发起 `fetch` 请求（超时 8 秒）
4. 解析/回退 → `game.aiThinking = false`（**谁设 true 谁设回 false**，在 `finally` 块中确保复位）
5. 重置计时器

**安全规则**：
- LLM 返回的部署坐标**不可信任**，拿到后必须先经过 `canDeployHere` 校验再调用 `deploy()`
- **坐标校验失败** → 调用 `fallbackDeployNear()` 就近偏移修正（只修坐标、不改决策）
- **网络失败/超时** → 重试 1 次；重试仍失败则本轮跳过，等待下次调度（不降级为本地决策）

### 4.7 update.js

**职责**：核心游戏逻辑，每帧调用一次，推进游戏世界状态。

**核心函数**：
- `update(deltaSec)` — 主流程（按顺序，见 8.1 节）；**由 main.js 以恒定 `FIXED_DELTA`（1/30 秒）调用**（Fixed Timestep，不再传变长 delta）
  - 开头固定步骤：`game.tick++` 逻辑帧计数 + 全实体 `prevX/prevY` 快照（渲染插值基准）

**辅助函数**：
- `findTarget(entity)` — 通用寻敌（区分对空/对地、建筑偏好），**跳过隐身/未露头单位**
- `findTargetInRangeForTower(tower, range)` — 塔专属寻敌
- `findHealTarget(healer)` — 治疗兵寻友（跳过主塔和堡垒）
- `attackTroop(attacker, target)` — 攻击逻辑（含溅射/连锁闪电/穿透箭/减速/现身/眩晕/冲锋倍率/治疗触发/群攻/光束灼烧/**浪人近战反弹**（`_reflectTimer`：格挡近战全额反弹，骑士冲锋3倍一并计入基数，🚫特效在攻击者头顶，远程弹道不触发）/标记等分支；近战/弹道伤害一律经 `calcActualDmg` 结算；主塔守卫为**堡垒同款追踪弹道**——命中目标才结算伤害）
- `moveToward(entity, tx, ty, deltaSec)` — 移动逻辑（含减速因子、后坐力缓动、狂暴加速）
- `rageMult(e)` — 狂暴增益倍率：`_rageTimer > 0` 时返回 1.3（狂暴buff对攻速/移速/蓄力/出兵/伤害等一切进度 +30%），否则 1.0
- `canTargetFlying(entity)` — 判断单位能否攻击飞行单位
- `getHitRadius(e)` / `getCollisionRadius(e)` / `isStaticEntity(e)` / `getMass(e)` / `sepShare(mine, other)` / `applySeparation()` — **同队碰撞分离系统**（见 5.7）
- `calcActualDmg(baseDmg, attacker, target)` — **伤害结算唯一收口**：内置狂暴加成（`rageMult(attacker)` ×1.3）+ 目标减伤（`_damageReduction` 系数）+ `floor` 取整 + **护盾拦截**（`target.shield > 0` 时本次伤害全由护盾吸收、不穿透生命，护盾归零后剩余伤害才扣血）；所有攻击/弹道/溅射/连锁等伤害必须经此函数，禁止绕过裸算（护盾机制详见 5.10）
- `spawnDmgNum(x, y, amount, isHeal)` — 伤害/治疗飘字

**规则**：
- **禁止在此文件中进行任何绘制操作**
- 所有实体状态变更（移动、攻击、生产）必须在此文件完成
- 死亡实体清理是本文件唯一权责
- **不得在此文件调用 `deploy` 或 `applySpellDamage`**（那些归 entities.js）

**圣水DOM更新特例**：`update.js` 中更新圣水数值的DOM显示（`document.getElementById(...).textContent = ...`）是已知的轻微职责越界（update 不该碰DOM），为减少冗余调用链而保留在此。但此特例**不构成先例**，后续任何新增DOM操作必须放进 `ui.js` 或专门的UI模块，禁止以"参照圣水那条"为理由继续在 update.js 中追加DOM操作。

### 4.8 render.js

**职责**：所有 Canvas 绘图操作，根据当前 `game` 状态渲染画面。

**核心函数**：
- `draw(alpha)` — 主绘制入口（内部末尾自动调用 `drawHoverUI()`）；开头做**渲染坐标插值投影**（把实体 x/y 临时投影到上一逻辑帧与当前逻辑帧之间，`try/finally` 恢复，净副作用为零）
  - ⚠️ 该投影是「render 只读不写」原则的登记特例（见原则 14），不构成先例
- `drawMainTower(b)` — 主塔绘制（旧名 `drawTowerBase` 已更名）
- `drawBastion(b)` — 堡垒绘制
- `drawBuilding(b, showRange)` — 建筑绘制（含各塔类特殊造型分支）
- `drawTroop(unit)` / `drawUnitBody(e)` — 兵种通用绘制
- 各兵种专属绘制函数：`drawXxx(unit)` 逐一命名（如 `drawKnight`, `drawMiner`, `drawDragonEgg`…，见 render.js 函数列表）
- `drawCopyUnit(e)` — 复制法术复制体（半透明造型）
- `drawDeployRing(item)` — 部署延迟转圈环
- `drawDeployZoneFrame(left, right, fade)` / `drawCrosshair(x, y, color)` — 部署预览（可部署区边框 + 鼠标范围圈/准心）
- `drawStatusIcon(entity)` — 头顶状态图标系统
- `drawLightningBolt(...)` / `drawDeployThunderbolt(...)` — 闪电/落雷绘制
- `drawHoverUI()` — 悬停信息

**规则**：
- **只读 `game` 状态，绝对不允许修改任何状态**
- 所有绘图函数使用全局 `window.ctx` 变量
- `draw()` 是 `main.js` 主循环中调用的**唯一绘制入口**——`drawHoverUI()` 只能在 `draw()` 内部作为最后一步被调用，`main.js` 不再单独调用它，防止任何人在两者之间插入逻辑打乱绘制顺序
- 虚线绘制用 `ctx.setLineDash([5,5])`，**用后必须重置** `ctx.setLineDash([])`，否则会污染后续绘制

### 4.9 ui.js

**职责**：处理玩家交互事件，连接输入与游戏逻辑。

**核心函数**：
- `setupUI()` — 注册所有事件监听器
- `renderCardPanel(mode)` — 渲染下方（蓝方）卡牌面板
- `renderTopCardPanel(mode)` — 渲染上方（红方）卡牌面板（**双人模式**）
- `refreshCardCooldowns()` / `refreshTopCardCooldowns()` — 每帧刷新双方卡牌冷却UI覆盖层
- `updateMirrorCardDisplay()` / `updateSingleMirror(selector, lastCardId)` — 镜像法术显示（追踪双方上次部署卡牌；拆分为单侧更新函数供上下两面板复用）
- `updateHover()` — 检测鼠标位置，更新 `game.hoveredEntity`
- `showBastionAlert()` — 显示堡垒爆破告警

**事件绑定**：
- 下方画布 `mousemove` → 更新 `game.mouseX/Y` + 调用 `updateHover()`
- 下方画布 `click` → 选中卡牌时调用 `deploy()`（双人模式下上方画布事件对称，走 `*2` 字段）
- 画布 `mouseleave` → 清除 `game.hoveredEntity`

**规则**：
- **所有事件监听器必须在 `setupUI()` 中注册**，分散注册易导致内存泄漏和冲突
- 状态**读取**在 render，状态**改变**在 ui（悬停状态等）
- 鼠标点击坐标需做 Canvas 坐标转换，**必须同时处理 X 和 Y 两个方向的缩放**（画布CSS尺寸与内部分辨率不一致时容易漏算Y轴）：

  ```js
  const rect = canvas.getBoundingClientRect();
  const scaleX = W / rect.width;
  const scaleY = H / rect.height;
  const clickX = (clientX - rect.left) * scaleX;
  const clickY = (clientY - rect.top) * scaleY;
  ```

### 4.10 deckManager.js

**职责**：卡组管理系统，localStorage 持久化存储。

**核心函数**：
- `getDecks()`, `setDecks()` — 读写所有卡组
- `getActiveDeck()`, `getActiveDeckCards()` — 获取当前激活卡组
- `saveDeck(deck)`, `deleteDeck(id)` — 增删卡组
- `setupDeckManager()` — 初始化 + 绑定主页按钮事件

**渲染函数**：
- `renderDeckList()` — 渲染卡组管理弹窗列表
- `openDeckEditModal(deck)` — 打开卡组编辑弹窗
- `renderCardSelection(selectedIds)` — 渲染卡牌选择网格

**规则**：
- 卡组上限 10 张卡
- 无卡组时自动创建默认卡组
- 删除当前激活卡组时自动切换到第一个

### 4.11 main.js

**职责**：程序入口，串联所有模块，启动游戏循环。

**核心逻辑**：
- 全局变量显式绑定：`window.canvas`, `window.ctx`
- 页面切换：主页 ↔ 游戏页（四种进入模式，见下表）
- AI配置管理弹窗的绑定和事件处理
- **人机选择弹窗的绑定和事件处理**：新建组（下载模板）/ 🔄 刷新（全量重新探测 js/ 目录，缺失自动移除）/ 关闭
- 导出/导入数据文件绑定：`exportUserDataFile` / `importUserDataFile`，导出前弹确认子界面防误触
- `gameLoop(ts)` — `requestAnimationFrame` 循环（**Fixed Timestep**）
  - **仅当 `gamePage` 可见时执行**：tick 累积器 → `update(FIXED_DELTA)` → `draw(alpha)`
  - 逻辑帧推进：`tickAccumulator` 累计真实时间（单帧 delta 上限 0.1s），每满 `FIXED_DELTA` 执行一次 `update(FIXED_DELTA)`（恒定步长）；`MAX_STEPS_PER_FRAME=5` 防螺旋死亡
  - **联机门控**：推进前调用 `canAdvanceTick()`（network.js），返回 false 时暂停逻辑推进（等待双方指令），渲染照常
  - 页面隐藏时自动挂起（`lastTimestamp` / `tickAccumulator` 置零），切回时不会爆炸式追帧
  - 这种"被动挂起"方案确保 AI 决策计时器随主循环一起停止，AI 不会在页面隐藏时继续决策
  - **渲染插值**：`draw(alpha)`，alpha = 距上一逻辑帧的进度（0~1），由 render.js 坐标投影呈现平滑移动
  - **帧率节流**：`fpsLimit > 0` 时按 `1000/fpsLimit` 间隔跳过中间帧（0=不限制），即时生效——**只管渲染帧率，逻辑恒 30Hz**

**页面按钮一览**（主页左侧两列：左列 🎮 单人模式；右列 ✨ 创意模式 + 🔗 联机模式，后两组均为占位按钮）：

| 分区 | 按钮 | 功能 |
|------|------|------|
| 单人模式 | 经典人机（卡组） | 经典模式（`gameMode='deck'`），仅卡组卡牌可用 |
| 单人模式 | 经典人机（全卡） | 经典模式（`gameMode='classic'`），全部卡牌可用 |
| 单人模式 | 经典双人（本机） | 本机双人模式（`gameMode='local_multi'`），上下双面板本地对战 |
| 单人模式 | 经典AI（LLM驱动） | LLM 模式（`gameMode='api'`），使用当前激活预设 |
| 创意模式 | 大地图人机（全卡） | **占位按钮（disabled）**：模式未实现 |
| 创意模式 | 攻城人机（全卡） | **占位按钮（disabled）**：模式未实现 |
| 联机模式 | 双人联机（全卡） | 联机模式（`gameMode='online'`）：VibeHub 房间 + Lockstep，双方使用全部卡牌 |
| 联机模式 | 双人联机（卡组） | 联机模式（`gameMode='online'`）：VibeHub 房间 + Lockstep，双方使用各自卡组。详见「联机架构.md」 |
| 配置区 | 🤖 人机选择 | 弹出AI行为组选择弹窗：选中即生效；「➕ 新建组」下载模板、「🔄 刷新」重新扫描 js/ 目录（缺失/未识别的自动移除） |
| 配置区 | 卡组管理 | 弹出卡组管理弹窗 |
| 配置区 | AI配置 | 弹出AI预设管理弹窗 |
| 配置区 | 💾 导出数据 | 弹出导出确认子界面（说明使用场景 + API Key 风险），确认后下载「個人データ.js」 |
| 配置区 | 📥 导入数据文件 | 选择「個人データ.js」，自动识别并恢复数据（浏览器本地数据优先） |
| ⚙️ 设置·帧率 | 主页设置区下拉框（不限制/60/30），localStorage 记忆（`towerwar_fps_limit`），即时生效；仅限制渲染帧率，逻辑恒 30Hz |

### 4.12 network.js

**职责**：联机网络层（VibeHub Room + Lockstep 延迟缓冲确定性同步；单机模式零影响）。完整设计见「联机架构.md」。

**核心函数**：
- `netCreateRoom(name, deck, cb)` / `netJoinRoom(roomId, name, deck, cb)` — 创建/加入房间（回调集：`onLobby` / `onGameStart` / `onDisconnect`）；房间号冲突自动换号重试
- `setOnlineReady(v)` / `hostStartOnlineGame()` — 准备同步 / 房主开局（下发 seed + 双方卡组）
- `dispatchCommand(cmd)` — **统一指令分发入口（Action Dispatcher）**：所有操作一律经此；单机立即执行并返回是否成功，联机转 `queueCommand` 走同步通道。指令格式 `{ type, ...payload }`；`type` 已注册 `'DEPLOY'`（team/cardId/x/y）、`'SKILL'`（team/skillKey），新类型必须在此登记
- `queueCommand(cmd)` — 本地指令入队 + 发送（联机必经通道）
- `onRemoteCommand(data)` / `executeDueNetCommands()` — 远端指令入队 / 到期指令批量执行
- `canAdvanceTick()` — **恒 true**：延迟缓冲方案无需 tick 级阻塞（指令按 execTick 对齐，天然一致）
- `isOnlineMode()` / `myOnlineTeam()` / `setNetworkEnabled(v)` — 联机模式判定 / 己方阵营 / 开关

**Lockstep 同步约定**（延迟缓冲）：
1. 双方各自 30Hz Fixed Timestep 推进；指令带 `genTick`，`execTick = genTick + 10`（≈333ms 缓冲）
2. `NET_PENDING_EXEC` 按 `(execTick, team: player先, seq)` 升序二分插入；`update()` 每 tick 执行到期指令
3. 同 seed + 同指令序列 + 确定性逻辑（`rand()`）→ 双端最终状态必然一致（乱序/迟到不影响）

**规则**：
- 不注册全局定时器/事件监听（遵循原则 3；心跳定时器随会话创建/销毁，见「联机架构.md」§6）
- ✅ **确定性前置已落地**：逻辑层全部 `Math.random()` 已替换为同种子 `rand()`（update.js 39 处 + entities.js 41 处 + ai配置 LLM 决策脚本 80 处）；render.js 渲染抖动保留 `Math.random()`（登记例外，见 4.3）
- **部署/技能操作唯一入口为 `dispatchCommand()`**（单机=直通、联机=入队）；禁止绕过它直接调用 `deploy()` / `castActiveSkill()` 扣圣水/建实体

---

## 五、共通机制（骨架描述）

### 5.1 部署延迟队列

所有卡牌部署不再即时生成实体，而是走**四步流水线**：

```
deploy() → 检查合法 → 扣费+设冷却 → 推入 game.deploying[] → update.js每帧倒计时 → finishDeployItem() 实际生成
```

- `deploy()` 只负责"入队列"，不负责"生成"
- `update.js` 每帧遍历 `game.deploying[]`，`timer -= deltaSec`，归零时调用 `finishDeployItem()`
- 每张卡牌有 `deployDelay` 属性（`config.js` 定义），决定部署到生成的时间差
- **两段式部署特例**（矿工类）：`finishDeployItem` 发现挖地类卡且未挖地时，先推入"土堆特效"到 `spellEffects[]`，再重新入队等待挖地时间，第二次到期才生成实体

### 5.2 卡牌冷却系统

- 每张卡牌有 `cooldown` 属性（单位：秒），部署后立即进入冷却
- `game.cardCooldowns{ player:{}, ai:{} }` 追踪各方的冷却状态
- `update.js` 每帧统一减少冷却值
- `ui.js` 的 `refreshCardCooldowns()` 读取冷却值并渲染UI覆盖层
- **冷却中的卡牌不可选中/部署**

### 5.3 状态图标系统

- `render.js` 的 `drawStatusIcon(entity)` 在实体头顶渲染状态图标
- 所有图标**一字排开居中显示**，每个图标间隔固定像素
- 内置状态图标（按绘制顺序，一字排开）：隐身(`_stealthed` → 🌫️)、减速(`slowTimer>0` 且非冰豆 → ❄️)、冰冻(`freezeTimer>0` → 🧊)、眩晕(`_stunTimer>0` → 💫)、加速(`_speedBoosted` → ⚡)、狂暴(`_rageTimer>0` → 😡)、冲锋(`_charging` → 🐴)、减伤(`_damageReduction>0` → 🛡️，免伤法徒护盾脉冲/巨人半血共用)、自回(`_hasRegen` → ❤️‍🩹)、虫标记(`_wormMarkTimer>0` → 🐛)、灼烧(`_burnTimer>0` → 🔥)
- **扩展方式**：在 `drawStatusIcon` 的 `icons` 数组中 `push` 新图标条件即可，排列逻辑自动适配

### 5.4 特效系统

游戏使用多种 **全局数据队列** 实现可视化特效，帧驱动更新：

| 队列 | 用途 | 更新逻辑 |
|------|------|---------|
| `projectiles[]` | 弹道（箭矢/炮弹/治疗弹/火箭/散射弹药等） | `update.js`: 追踪制或直线飞行 → 命中/到点移除 |
| `spellEffects[]` | 通用法术特效（爆炸/箭雨/土堆等） | `update.js`: `timer -= deltaSec` → 归零移除 |
| `dmgNumbers[]` | 伤害/治疗飘字 | `update.js`: 上飘 + 淡出 |
| `lightningChains[]` | 连锁闪电路径 | `update.js`: `timer -= deltaSec` → 归零移除 |
| `deployLightnings[]` | 部署落雷（雷电法师类） | `update.js`: 计时移除 |
| `deployEffects[]` | 部署范围冲击（超级骑士类） | `update.js`: 计时移除 |
| `pierceArrows[]` | 穿透箭 | `update.js`: 分段碰撞检测 + 标记已击中实体 |
| `speedZones[]` | 极速法术加速区域 | `update.js`: 计时移除 + 范围内友军加速 |
| `rageZones[]` | 狂暴法术狂暴区域 | `update.js`: 计时移除 + 每 `rageTick`(0.5s) 脉冲对范围内友军施加 `boostDuration`(1.5s) 狂暴buff（`_rageTimer`） |
| `freezeZones[]` | 冰冻法术冰封区域 | `update.js`: 计时移除（纯展示） |
| `arrowRainStrikes[]` | 箭雨多段延迟伤害 | `update.js`: 分段触发 + 特效 |
| `fireballStrikes[]` | 火球半空下坠延迟结算 | `update.js`: 落地伤害 + 击退 + 特效 |
| `earthquakeStrikes[]` | 地震多段延迟伤害 | `update.js`: 分段触发（对建筑高倍率） |
| `thunderStrikes[]` | 大雷电逐道落雷结算 | `update.js`: 按锁定顺序逐道劈下 |
| `bombs[]` | 攻城人💣倒计时爆炸 | `update.js`: `timer -= deltaSec` → 归零时范围伤害 + 特效 |
| `fishingLines[]` | 渔夫甩钩鱼线（蓄力/飞行/拖拽可视化） | `update.js`: 计时移除；关联渔夫死亡或钩子结束时一并移除 |

### 5.5 特殊标记字段系统

实体通过**标记字段**实现特殊行为，而非新增 type：

| 标记字段 | 所属实体 | 作用 |
|---------|---------|------|
| `fortification: true` | 主塔、堡垒 | 防御工事统一标记：法术伤害应用系数、不可被治疗 |
| `isSiege: true` | 攻城人类 | 触碰建筑自爆、死亡留下💣 |
| `_turretMode` | 炮车 | 炮车被打爆后原地变形成炮台建筑（第二条命，须在死亡结算阶段转换，见原则 15） |
| `_iceBean: true` / `_fireBean: true` | 冰豆 / 火豆 | 自爆类单位 |
| `_stealthed: true` | 幽灵、突袭单位 | 隐身（不可被锁定），攻击后现身，超时恢复 |
| `_selfDestructed: true` | 自爆类（已爆后） | 防重复触发自爆逻辑 |
| `spawnTimer` | 女巫类/兵营 | 周期性召唤计时 |
| `freezeTimer` | 冰冻单位 | 冻结中：无法移动/攻击/蓄力/召唤 |
| `_stunTimer` | 眩晕单位 | 眩晕中：暂停行动 |
| `_speedBoosted` | 加速单位 | 极速法术加速状态 |
| `_shieldTimer` / `_shieldPulseTimer` | 护盾类 | 免伤护盾计时（法徒类脉冲施加） |
| `_damageReduction` | 减伤单位 | 伤害减免状态 |
| `_charging` / `_chargeTimer` | 冲锋类 / 蓄能类 | 冲锋或蓄能进度 |
| `_leapCharging` / `_leapTimer` | 跳跃类 | 蓄力跳跃中：不攻击不移动 |
| `_assaultCharging` / `_assaultTimer` | 突袭类 | 突袭冲刺中：短暂隐身 |
| `_recoilTimer`（+`_recoilVx/_recoilVy`） | 后坐力单位 | 开火后坐力缓动 |
| `_kbX` / `_kbY` | 被击退单位 | 击退一次性位移（帧驱动衰减归零，见 8.1） |
| `_headHidden` / `_headTimer` | 未露头单位 | 不可被锁定，AOE 仍可波及；`_headTimer` 为露头倒计时（电磁塔射程内有敌人才露头，超时缩回） |
| `_isSpawned` / `lifetime` | 召唤物 | 召唤物生命周期倒计时 |
| `_souls` / `_level` / `_soulsPerLevel` / `_maxLevel` / `_baseHp` / `_baseAtk` | 灵魂升级类 | 击杀计数升级（升级按基础血攻成长） |
| `_isEgg` | 蛋形态单位 | 蛋形态 → 满血孵化成另一形态（飞行单位等） |
| `_eggPulseTimer` / `_hasRegen` | 蛋/自回类 | 孵化脉冲计时 / 常驻自回buff（❤️‍🩹图标） |
| `_healTicks` / `_healActive` / `_healTickTimer` / `_healAmount` | 治疗脉冲类 | 登场/攻击触发的持续治疗脉冲（剩余次数/窗口/间隔/单次量） |
| `_rageTimer` | 狂暴单位 | 狂暴buff剩余时间（😡，全进度+30%） |
| `_reflectTimer` | 浪人 | 近战反弹冷却（格挡近战全额反弹，冷却中不触发） |
| `_wormMarkTimer` | 巫师虫标记单位 | 虫标记倒计时（🐛，到期生成小虫） |
| `_burnTimer` / `_burnDamage` / `_burnAccumulator` | 灼烧单位 | 灼烧状态（🔥）：倒计时 / 每秒灼烧伤害 / 伤害累计器（按帧累计满 1 点才结算，避免浮点漂移） |
| `_beamTimer` / `_beamTargetId` / `_beamSwitchCooldown` | 光束类（地狱塔） | 光束灼烧计时 / 锁定目标 / 切换目标冷却 |
| `_leapTargetId` / `_assaultTargetId` | 跳跃/突袭类 | 蓄力期间锁定的目标（无敌期间不可被切换） |
| `_speedBoostTimer` | 加速单位 | 极速加速剩余计时（`_speedBoosted` 为状态标记） |
| `_soulCounted` | 灵魂升级类 | 防止同一击杀重复计入灵魂计数 |
| `_hookTimer` / `_hookCharging` / `_hookTargetId` / `_hookLineId` / `_fishingLineSeq` / `_hookPulling` / `_hookFlying` / `_pullTimer` / `_pullToX` / `_pullToY` | 渔夫 | 钩子状态机：蓄力 / 锁定目标 / 关联鱼线 / 甩钩飞行 / 收线拖拽至目标点 |
| `isCopy: true` | 复制法术产物 | hp=1 复制体，渲染层半透明亮蓝建模，禁止二次复制 |
| `_patrolX` / `_patrolY` / `_patrolDir` | 主塔守卫 | 巡逻圆心（己方主塔坐标）与绕圈方向（顺/逆时针随机）；未记录时兜底自动查找己方主塔 |

> **主塔/堡垒**：不再使用 `isMainTower` / `bastion` 标记字段，统一以 **type**（`'main_tower'` / `'bastion'`）+ `fortification` 标记表达。
> **扩展**：新增特殊行为时，优先考虑用标记字段 + `update.js` 中的条件判断，避免新增实体类型。

### 5.6 卡组系统

- 卡组存储在 `localStorage`，上限 10 张卡
- `deckManager.js` 提供完整的增删改查 + 渲染
- 两种使用场景：
  - **卡组对战模式**（`gameMode='deck'`）：仅玩家受卡组限制
  - **AI对战模式**（`gameMode='api'`）：双方都受卡组限制
- `deploy()` 中通过卡组校验锁防止绕过

### 5.7 同队碰撞分离系统

- 目的：防止同队单位互相叠加堆挤
- 实现：`update.js` 每帧调用 `applySeparation()`，对同队且非静态的实体两两计算分离
- 参数化：`getCollisionRadius(e)` 取碰撞半径、`getMass(e)` 取质量（决定推动权重）、`isStaticEntity(e)` 判断建筑等不可推动实体、`sepShare(mine, other)` 计算双方分离份额
- **约束**：实体坐标的每帧推进只允许发生在 `update.js`（移动/碰撞分离），其他文件不得直接修改实体坐标

### 5.8 双人本地模式（gameMode='local_multi'）

- 定位：本机双人测试模式，是未来联机的本地雏形
- 双面板 UI：下方蓝方（原玩家面板）、上方红方（`renderTopCardPanel` / `refreshTopCardCooldowns`）
- 状态字段：红方交互走 `game.uiState.selectedCardId2`（本地 UI 状态，见 4.2）；镜像追踪 `lastDeployedCardId2` 为顶层战斗数据
- **AI 调度跳过**：`update.js` 在 `local_multi` 下不推进 `aiDecisionTimer`，不调用任何 AI
- 红方圣水显示由 `update.js` 帧驱动更新（与蓝方圣水同属已知 DOM 特例）

### 5.9 数据持久化与备份（個人データ.js）

- 定位：可选数据文件（根目录「個人データ.js」），删除不影响游戏运行
- 日常数据（AI预设含 API Key、卡组、设置）自动存浏览器 `localStorage`，无需手动操作；本文件仅作备份/搬家用途
- **导出**：主页「💾 导出数据」→ 弹导出确认子界面（防误触，说明使用场景与风险）→ 确认后下载本文件（`doExportUserDataFile`）
- **导入**：主页「📥 导入数据文件」→ 选择本文件 → `extractUserdataObject` 自动识别内容并恢复
- 优先级：浏览器本地数据优先于文件数据；文件加载紧随 `config.js`，暴露 `window.TOWERWAR_USERDATA` 供 apiAI.js 读取
- **安全声明**：文件含 API Key 密钥；分享游戏前必须删除本文件，否则对方打开即可见密钥

---

### 5.10 护盾通用机制（`shield` / `maxShield`）

护盾 = 实体**第二个血条**，与生命条**不连贯**（结算独立、渲染为覆盖生命条的第二层）。核心规则：

1. **伤害优先扣盾、不穿透生命**：一切**外部伤害**（普攻/弹道/溅射/法术/AOE/持续伤害/反弹等）都先扣 `shield`，`shield > 0` 时本次伤害**全由护盾吸收**、绝不扣 `hp`；
   > **例外——自发扣血不受护盾影响**：实体**自身**的持续扣血机制（自燃/自流血：地狱塔 25HP/s、电磁塔 10HP/s、十字弩 24HP/s）直接扣 `hp`、不经过伤害结算，护盾拦不住；哪怕带盾，血量照样慢慢扣（这是自发消耗，与外部伤害无关）。
2. **破碎时完整挡下一次伤害**：某次伤害导致护盾归零（破碎）时，**该次伤害同样被完整挡下**（超出部分吞掉、不穿透生命）；护盾归零后，后续伤害才正常扣生命；
3. **不受血量治疗影响（隔离）**：医疗兵/战斗天使/自回/治疗法术等一切血量治疗**只回复 `hp`、永不回复 `shield`**；盾不满但血满的单位不会吸引医疗兵（`findHealTarget` 只找 `hp < maxHp` 的目标）。

**统一收口**：所有**外部伤害**路径统一走 `calcActualDmg(baseDmg, attacker, target)`（狂暴加成 → 目标减伤 → 护盾拦截），禁止任何地方直接扣 `hp` 绕过护盾（**自发扣血类除外**：自燃/自流血直接扣 `hp` 是合法豁免，见规则 1 例外）。

**字段与接口**：
- `shield`（当前护盾）/ `maxShield`（护盾上限）：由 `createEntity` 兜底创建（无盾为 0）；带盾卡牌只需在配置加 `shield` 即自动生效（如守卫骷髅 80）；
- `grantShield(target, amount)`：通用加盾接口（不超过 `maxShield`；原本无盾则以本次加盾量为上限），未来加盾卡牌/法术统一走这里。

**渲染**：盾条作为第二层血条覆盖在生命条上方（盾满时全蓝覆盖，随消耗缩短）。

**复制体规则**：只有**本体带盾**的单位的复制体才有 1 点盾（无盾单位不白送），且复制体盾锁定 ≤ 1（每帧钳制）。

**护盾破碎触发**：护盾归零（破碎）的瞬间可触发额外效果（如主塔盾破时一次性召唤主塔守卫，生成于主塔周围）。属**触发源扩展**——护盾吸收/结算规则不变，召唤出的守卫按普通实体参与游戏（寻敌/移动/攻击均走通用逻辑）。

> 与法徒免伤 `_shieldTimer`（完全免伤计时，护盾脉冲施加）是**两套独立机制**，互不影响。

### 5.11 联机前置机制（v4.7 落地）

定位：为未来 Lockstep 联机铺路的**确定性 / 对称性改造**。单机行为零变化（已回归验证），全部为对现有结构的收敛与登记。

| 机制 | 落点 | 说明 |
|------|------|------|
| 阵营对称数据 | `config.js` / `gameState.js` / `utils.js` | `elixir{player,ai}`、`elixirMultiplier{player,ai}`、`bastionsLost{player,ai}` 字典化；`TEAMS` 常量 + `opponentTeam(team)` 获取对方阵营键 |
| 确定性随机 | `utils.js` / `gameState.js` | Mulberry32 种子随机 `rand()`/`setRandomSeed()`；逻辑层全部替换（update.js 39 处 + entities.js 41 处 + ai配置 80 处）；`resetGame(seed)` 开局对齐种子、两端序列一致 |
| 指令分发 | `network.js` | `dispatchCommand()` 统一入口；类型 `DEPLOY`（team/cardId/x/y）、`SKILL`（team/skillKey），单机直通、联机入队；新操作类型必须在此注册 |
| 状态分离 | `gameState.js` | 本地 UI 状态收进 `game.uiState`（不参与同步）；`getBattleStateSnapshot()` 导出可 JSON 序列化的纯净战场快照 |
| 坐标翻转抽象 | `utils.js` | `worldToScreen` / `screenToWorld`（当前恒等映射，未来双视角上下镜像适配预留） |

**确定性红线**：逻辑帧内任何随机必须 `rand()`；`Math.random()` 仅限 render.js 视觉抖动（登记例外）。

### 5.12 联机子系统（gameMode='online'，v11.64 落地）

VibeHub Room（WebRTC P2P / VibeNet 中继）+ Lockstep 延迟缓冲同步（非旧骨架的 tick 级门控，`canAdvanceTick` 恒 true）。

- **角色**：Host=蓝方(player)，Client=红方(ai)；`canOperateTeam(team)` 守卫（ui.js）保证只能操作己方面板
- **同步**：指令带 `genTick`，`execTick=genTick+10` 延迟缓冲；执行序 `(execTick, player先, seq)` 双端一致；`update()` 每 tick 调 `executeDueNetCommands()`
- **游戏内适配**：online 模式 AI 决策段跳过；双人圣水/卡牌冷却 DOM 同步刷新；技能走 SKILL 指令；烟引卡联机过滤
- **协议/状态机/断线/测试**：详见「联机架构.md」（独立文档，框架仅存指引）

---

## 六、文件依赖关系与加载顺序

> **两张图的区别**："依赖关系图"表示"谁的函数调用了谁"（逻辑依赖），"加载顺序"表示 `<script>` 标签的物理顺序。两者数值上一致（加载顺序 = 依赖顺序的拓扑排序），但概念上分开理解：加载顺序保证的是"变量已定义"，不代表"此时可以调用"（例如 render.js 加载时 `ctx` 还未赋值，但绘制函数在 main.js 的循环中才被调用，届时 `ctx` 已就位，因此安全）。

### 6.1 依赖关系图（谁调用谁）

```text
config.js            （无依赖，提供全局常量）
  ↓
gameState.js         （依赖 config，提供 game 可变状态）
  ↓
utils.js             （纯函数，只读 config 常量，不读写 game）
  ↓
entities.js          （依赖 config, gameState, utils）
  ↓
ai测试组.js / ai哥布林组.js  （无依赖，各自定义 window.AIGroupXxx 行为组对象）
  ↓
aiSelector.js        （依赖内置行为组对象 + localStorage，提供注册表/人机选择/决策调度）
  ↓
apiAI.js             （依赖 config, gameState, utils, entities）
  ↓
network.js           （依赖 gameState 读 game.gameMode；提供 canAdvanceTick 门控，被 main.js 调用）
  ↓
update.js            （依赖 config, gameState, entities, ai, apiAI, utils）
  ↓
render.js            （依赖 config, gameState — 只读）
  ↓
ui.js                （依赖 config, gameState, entities, render）
  ↓
deckManager.js       （依赖 config, gameState — 自包含）
  ↓
main.js              （依赖以上所有，启动入口）
```

### 6.2 index.html 中的 script 加载顺序

```html
<script src="js/config.js"></script>
<script src="個人データ.js"></script>
<script src="js/gameState.js"></script>
<script src="js/network.js"></script>
<script src="js/utils.js"></script>
<script src="js/entities.js"></script>
<script src="js/ai测试组.js"></script>
<script src="js/ai哥布林组.js"></script>
<script src="js/aiSelector.js"></script>
<script src="js/apiAI.js"></script>
<script src="js/update.js"></script>
<script src="js/render.js"></script>
<script src="js/ui.js"></script>
<script src="js/deckManager.js"></script>
<script src="js/main.js"></script>
```

> **安全声明**：`render.js` 代码中直接使用 `ctx`，但由于绘制函数在 `main.js` 的 `requestAnimationFrame` 中才被调用，此时 `window.ctx` 已就位，顺序安全。

---

## 七、实体系统约定

### 7.1 所有实体共有字段

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | number | 唯一标识，自增 |
| `type` | string | 实体类型：`troop` / `tower` / `barrack` / `collector` / `healer` / `main_tower` / `bastion` |
| `team` | string | `'player'` 或 `'ai'` |
| `cardId` | string | 对应卡牌ID，用于查询 `CARDS`（内置建筑用自身名） |
| `x, y` | number | 像素坐标（逻辑坐标；渲染时被插值投影临时覆盖） |
| `prevX, prevY` | number | 上一逻辑帧位置（渲染插值基准；createEntity 兜底 = 出生点，update.js 每逻辑帧开头快照） |
| `hp` | number | 当前生命值（`hp <= 0` 视为已死亡） |
| `maxHp` | number | 最大生命值 |
| `shield` | number | 当前护盾（第二血条，所有实体兜底存在，无盾为 0；机制详见 5.10） |
| `maxShield` | number | 护盾上限（无盾为 0；机制详见 5.10） |
| `hitRadius` | number | 受击半径（匹配视觉半径，贴边即可被攻击/命中） |
| `fortification` | bool | 防御工事标记（主塔/堡垒：法术伤害系数、不可治疗） |

### 7.2 各类型特有字段骨架

| 类型 | 字段 | 说明 |
|------|------|------|
| **troop** | `moveSpeed`, `atk`, `atkSpeed`, `atkCooldown`, `range`, `targetMode`, `targetId`, `flying`, `splash`, `slowFactor`, `slowTimer` | 地面/飞行兵种。`targetId` 由 `findTarget` 写入；可选 `groundOnly`/`pierce`/`shotCount`/`canHitAir` 等 |
| **tower** | `atk`, `atkSpeed`, `atkCooldown`, `range`, `splash`, `onlyGround` | 防御塔。可附加特殊行为字段（光束灼烧、眩晕、变形等） |
| **barrack** | `spawnTimer`, `spawnInterval`, `spawnCount`, `spawnUnit` | 兵营，定时生成小兵 |
| **collector** | `generateTimer`, `generateInterval` | 圣水生成器，定时产生圣水 |
| **healer** | `healAmount`, `healSpeed`, `healCooldown`, `range`, `targetId` | 治疗兵，**只治疗不攻击**。`targetId` 只能由 `findHealTarget` 写入 |
| **main_tower / bastion**（内置建筑） | `fortification`, `hitRadius`；堡垒另有 `atk`, `atkSpeed`, `range` | 开局由 `resetGame()` 创建；胜负判定依据主塔实体 hp |

> **注释**：新增类型时，必须在 `createEntity` 中处理默认值，并在 `update.js` / `render.js` 添加对应逻辑。若新类型既能攻击又能治疗（如未来的"英雄"），**必须拆分为两个独立字段**（如 `attackTargetId` 与 `healTargetId`），禁止复用同一个 `targetId` 字段表达两种不同语义——因为 `findTarget`（寻敌）和 `findHealTarget`（寻友）写入同一个字段会互相覆盖，且 render 无法判断当前显示的是攻击目标还是治疗目标。

---

## 八、核心游戏循环流程

### 8.1 每逻辑帧流程（Tick，按执行顺序）

> 每逻辑帧 = 固定步长 `FIXED_DELTA`（1/30 秒），由 main.js 累积器驱动；联机模式下需等双方当前 tick 指令齐备（`canAdvanceTick()` 为 true）才推进。

```
0. 逻辑帧计数 game.tick++ + 全实体 prevX/prevY 快照（渲染插值基准）
1. 圣水回复（各边独立速率，丢堡帮扶倍率）
2. AI决策调度（经典 2.0s / LLM 2.5s；双人模式跳过）
3. 卡牌冷却递减
4. 部署延迟队列倒计时 → 生成实体（含两段式挖地特例）
5. 区域法术场更新（加速区域 → **狂暴区域脉冲**（每 `rageTick` 对范围内友军施加 `boostDuration` 狂暴buff）→ 冰封区域计时）
6. 遍历实体更新（生产 → 采集 → 塔类攻击/光束灼烧 → 特殊单位行为 → 寻敌 → 移动/蓄力/突袭 → 攻击）
   > **特殊单位行为示例——主塔守卫巡逻**：以己方主塔为圆心（`_patrolX/_patrolY`，未记录时兜底查主塔）绕圈巡逻；**只索敌巡逻圈 + 自身射程范围内的敌人**（索敌范围 = 巡逻圈 + 射程，固定值，不与法师塔攻击范围耦合）：圈内有敌 → 出击（交给通用索敌/移动/攻击逻辑），圈内无敌 → 回内圈继续巡逻（巡逻中跳过通用索敌与攻击）；巡逻绕圈方向 `_patrolDir` 随机顺/逆时针，径向回圈修正防走偏
7. 弹道更新（追踪制 / 直线 / 分裂 / 散射）
8. 穿透箭分段碰撞检测
9. 通用法术特效 + 伤害飘字计时
10. 💣倒计时爆炸
11. 多段延迟法术结算（箭雨 / 地震 / 大雷电 / 火球下坠）
12. 闪电链 / 落雷 / 范围冲击特效更新
13. 胜负判定（主塔实体 hp）
14. 同队碰撞分离（防叠加）
15. 建筑特例（牢笼破损出怪、炮车被打爆变形成炮台）——须在死亡清理前
16. 死亡召唤与死亡结算（死亡召唤 / 通用死亡爆炸 / 自爆 / 灵魂升级）——须在死亡清理前
17. 统一清理死亡实体（唯一清理点）
18. 堡垒爆破检测 → 更新丢堡数 + 圣水帮扶倍率 + 告警
19. UI更新（圣水显示 → 双人红方圣水 → 卡牌冷却覆盖层）
```

### 8.2 事件驱动路径（与帧驱动并行）

- **鼠标移动** → `ui.js` 更新 `game.mouseX/Y` + `game.hoveredEntity`
- **鼠标点击** → `ui.js` 调用 `entities.js` 的 `deploy()`（双人模式上下两方对称）
- **AI决策** → 经典/LLM 模式均通过帧驱动调度，最终调用 `entities.js` 的 `deploy()`

### 8.3 绘制流程（rAF 渲染 + 插值）

```
draw(alpha):
  ① 实体坐标插值投影（prevX/prevY → 当前 x/y 按 alpha 插值；finally 恢复）
  清屏 → 背景(阵营色) → 河流 → 堡垒竖虚线 → 部署延迟转圈环
  → 绘制所有实体 → 状态图标 → 弹道特效 → 穿透箭 → 加速区域 → 冰封区域
  → 通用法术特效 → 💣 → 闪电链 → 落雷 → 范围冲击 → 伤害飘字
  → 部署预览(可部署区浅光框 + 鼠标范围圈/准心) → 结束画面覆盖(若gameOver)
  → 悬停UI(末尾)
```

---

## 九、全局命名规范

- 全局变量：小驼峰，如 `game`, `entityIdCounter`
- 常量：大写加下划线，如 `W`, `H`, `CARDS`, `PLAYER_TOWER`
- 函数命名：
  - `get` 前缀：查询操作
  - `is` / `can` 前缀：布尔判断，如 `isInHalf`, `canDeployHere`, `isStaticEntity`
  - `find` 前缀：寻找目标，如 `findTarget`, `findHealTarget`
  - `draw` 前缀：绘制相关
  - `update` 前缀：更新逻辑（`updateHover`）
  - `create` 前缀：实体/子单位创建（`createEntity`, `createBat`…）
  - `moveToward` / `attackTroop` 等动词：具体动作

---

## 十、新增文件的标准流程

1. 确定新文件职责，检查是否可归入已有文件（避免过度碎片化）
2. 在 `js/` 目录创建新文件，遵守单一职责
3. 在 `index.html` 中按依赖顺序插入 `<script>` 标签（在 `main.js` 之前）
4. 在 `main.js` 中注册/调用新文件的初始化函数
5. 测试整体运行，确保未引发全局变量冲突或加载顺序错误
6. 更新本文档的"文件职责"及"依赖顺序"章节

---

## 十一、性能与扩展性说明

### 11.1 实体上限
当前设计支持约 200 个实体流畅运行（60fps）。超过 400 时低性能设备可能卡顿。

### 11.2 扩展的三种层级

| 层级 | 需求 | 操作范围 | 难度 |
|------|------|---------|------|
| **Ⅰ 数值调整** | 改血量/费用/攻速等数值 | 只改 `config.js` 中的 `CARDS` 数值 | 低 |
| **Ⅱ 新增武将/法术**（已有type） | 新增兵种、法术 | 只改 `config.js` 添加 `CARDS` 条目；死亡爆炸/死亡召唤/部署法术等通过加字段即可获得对应机制 | 低 |
| **Ⅱ+ 新增特殊行为**（已有type） | 新兵种带新机制 | `config.js` 加字段 + `entities.js` 部署初始化 + `update.js` 帧更新 + `render.js` 专属绘制 | 中 |
| **Ⅲ 新增卡牌类型**（新type） | 如"陷阱""英雄" | 改 `entities.js`(创建逻辑) + `update.js`(帧更新) + `render.js`(绘制方法) | 高 |
| **特殊绘制** | 某兵种有独特造型 | 在 `render.js` 添加专属 `drawXxx` 分支 | 中 |
| **新增特效** | 粒子/光效等 | 在 `render.js` 添加绘制 + `update.js` 添加更新 | 中 |
| **网络对战** | 远程联机 | `network.js` 骨架已就位（`canAdvanceTick` Lockstep 门控 + 指令队列接口）；可复用双人模式的双面板结构；**联机前须将 update.js 全部 `Math.random()` 替换为同种子随机** | 高 |

---

## 十二、不可违背的原则（强约束）

以下原则由历史开发经验总结，任何修改不得违反：

1. **`utils.js` 不得读写 `game` 对象** — 只能读全局常量数据和函数参数
2. **死亡实体清理的唯一权责在 `update.js`** — entities.js 只标记 `hp<=0`，不做数组移除
3. **AI 不能注册自己的定时器/事件监听** — 唯一触发点是 `update.js` 的帧驱动计时器
4. **AI 必须通过 `deploy()` 部署** — 不许绕过部署入口直接创建实体或扣圣水
5. **`render.js` 只读不写** — 禁止修改任何 `game` 状态
6. **`update.js` 的圣水DOM更新是唯一历史特例** — 后续新增DOM操作必须放 `ui.js`
7. **主塔血量只能由 entities.js(法术) 和 update.js(攻击) 修改** — 指 `entities[]` 中主塔实体（`type:'main_tower'`）的 `hp`，其他文件禁止改动
8. **`draw()` 是 main.js 主循环中调用的唯一绘制入口** — `drawHoverUI()` 在其内部末尾自动调用
9. **治疗兵的 `targetId` 只能由 `findHealTarget` 写入** — 与 `findTarget` 的语义不可混用
10. **`deploy()` 中费用检查和位置检查不交叉重复** — 费用检查归 `deploy`，位置检查复用 `canDeployHere`
11. **实体坐标每帧推进只允许在 `update.js`** — 移动、后坐力、碰撞分离（`applySeparation`）都归帧驱动；其他文件不得直接改实体坐标
12. **防御工事（`fortification`）不可被治疗** — 治疗逻辑（`findHealTarget` / 治疗脉冲）必须跳过主塔与堡垒
13. **所有伤害结算必须经 `calcActualDmg(baseDmg, attacker, target)`** — 狂暴加成（+30%）与目标减伤（`_damageReduction`）在此统一收口，且 `target.shield > 0` 时伤害先由护盾吸收、不穿透生命（见 5.10）；禁止绕过函数裸算伤害；特殊情况（如反弹直接结算）也需显式调用该函数
14. **render.js 的 `draw(alpha)` 渲染坐标插值投影是「只读不写」的登记特例** — 开头临时把实体 x/y 投影到插值位置、`finally` 恢复，净副作用为零；与 update.js 圣水 DOM 特例对等，**不构成先例**，禁止以"投影都能改坐标"为由在 render.js 其他函数中修改状态
15. **`update(FIXED_DELTA)` 的唯一调用入口是 main.js 的 gameLoop 累积器** — 逻辑恒 30Hz 固定步长；`canAdvanceTick()` 返回 false（联机等待双方指令）时不得推进逻辑帧
16. **逻辑层随机一律走 `utils.js` 的 `rand()`** — 同种子序列一致是 Lockstep 联机确定性的根基；`Math.random()` 仅限 render.js 视觉抖动（登记例外，见 4.3），严禁在 update.js / entities.js 的逻辑路径使用
17. **所有部署操作必须经 `network.js` 的 `dispatchCommand()`** — 单机直通执行、联机入队同步；禁止绕过指令入口直接调用 `deploy()` 扣圣水/建实体（对原则 4 的收口：AI 与玩家同等受约束）

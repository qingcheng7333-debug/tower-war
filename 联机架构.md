# 🔗 联机架构（VibeHub 双人联机·全卡 / 卡组模式）

> **定位**：记录联机系统的架构设计、同步模型、消息协议，以及把改动同步到 VibeHub 平台的标准方法。
> 本文档是骨架级规范：不记版本号、不记测试结果、不记进度状态。
> 本文档参考 `基础框架.md` 及当前项目实现；VibeHub 官方 CLI 规则以官方接口最新内容为准。

---

## 1. 总体架构

**VibeHub SDK v3 Room（WebRTC P2P / VibeNet 中继）+ Lockstep 确定性同步（延迟缓冲）**

```text
┌──────────────┐       VibeHub Room        ┌──────────────┐
│  Host（房主） │ ◄── room.send() 消息 ──► │ Client（加入）│
│  蓝方 player  │   WebRTC / VibeNet       │  红方 ai     │
└──────────────┘                          └──────────────┘
```

- Host 是蓝方 `player`，Client 是红方 `ai`。
- 双方各自运行一份完整游戏逻辑，不把实时战场状态写入服务器。
- 双方只通过 VibeHub Room 交换大厅消息和游戏操作指令。
- VibeHub SDK 负责 WebRTC 连接、VibeNet 中继、连接状态和自动重连。
- 游戏使用固定 30Hz 逻辑帧、同步随机种子和确定性 `rand()`。
- 当前同步模型为 **lockstep**：适合本游戏严格的双人对战和确定性逻辑。

### VibeHub 平台约束

- 必须使用 `window.VibeHub` SDK。
- SDK 使用稳定版 v3，并通过绝对地址加载：
  `https://vibe.lumigrav.space/sdk/v3/vibehub.js`
- 禁止自建后端、数据库、WebSocket 和实时状态轮询。
- 实时操作通过 `room.send()` 进行 P2P 传输。
- `room.data`、`vibe.save` 和 `vibe.global` 不用于保存高频战斗状态。
- 公开房间发现使用 `vibe.rooms.list()`。

---

## 2. 文件分工

| 文件 | 职责 |
|---|---|
| `js/vibehubAdapter.js` | 初始化 VibeHub SDK、识别作品 slug、处理登录/退出和账号状态 |
| `js/network.js` | VibeHub Room 创建/加入/离开、大厅消息、游戏指令、Lockstep 队列、连接状态 |
| `js/main.js` | 联机入口、大厅 UI、登录检查、房间列表、开始对局、断线回主页 |
| `js/ui.js` | 阵营操作限制、卡牌面板、技能指令、联机禁用烟引卡 |
| `js/update.js` | 固定 30Hz 游戏推进、执行到期联机指令、在线模式跳过 AI |
| `js/gameState.js` | 游戏状态和随机种子重置；不保存网络连接对象 |
| `index.html` | 加载 VibeHub SDK 和游戏脚本，提供登录区及联机大厅 |
| `css/style.css` | VibeHub 账号区、联机大厅和连接状态样式 |

---

## 3. 平台初始化与登录

1. 游戏在 VibeHub 作品地址中从路径取得项目 slug。
2. 通过 `VibeHub.init({ work: slug })` 初始化 SDK。
3. VibeHub 作品页面显示登录、退出和当前账号状态。
4. 单机模式不要求登录；联机模式要求登录。
5. 登录凭证只由 SDK 保存在当前页面内存中，不写入 `localStorage` 或 `sessionStorage`。
6. 账号退出或授权失效时，游戏停止继续使用当前联机房间，并返回登录提示。

---

## 4. 房间与大厅流程

```text
未进入房间
  ├─ 房主：生成房间号 → VibeHub join → announce 公共房间
  └─ 加入者：输入房间号或查看公开房间 → VibeHub join
          ↓
      HELLO / JOIN_ACK
          ↓
      双方准备
          ↓
      房主发送 GAME_START
          ↓
        进入对局
```

- 房间使用 VibeHub `room.join(roomId, { topology: 'mesh' })`。
- 房主使用 `room.announce()` 发布低频房间信息。
- 公开房间列表使用 `vibe.rooms.list()`，只用于发现房间。
- 当前房间人数上限为 2 人。
- 房主关闭或玩家离开时调用 `room.leave()`。
- VibeHub SDK 负责连接重连和中继切换，游戏不实现自定义心跳。

### 房间状态

```text
idle → hosting → 对手加入 → 双方准备 → in_game
idle → joined  → 收到房主确认 → 双方准备 → in_game
```

对应状态值：

```text
idle / hosting / joined / in_game
```

### 握手可靠性

`room.join()` resolve 时 WebRTC DataChannel 不一定已建立，此时发出的首条 `HELLO` 可能被静默丢弃。因此 Client 采取以下兜底策略（仅握手阶段，不属于对局心跳）：

- 收到 peer `join` 事件时立即补发 `HELLO`；
- 每 1.5 秒重发一次，最多 20 次（约 30 秒超时）；
- 收到 `JOIN_ACK` 后停止重发。

---

## 5. 大厅消息

所有大厅消息通过 `room.send()` 传输。

| 消息 | 方向 | 作用 |
|---|---|---|
| `HELLO` | Client → Host | 发送昵称和卡组 |
| `JOIN_ACK` | Host → Client | 确认加入并同步双方卡组 |
| `LOBBY_READY` | 双向 | 同步准备状态 |
| `GAME_START` | Host → Client | 同步随机种子、昵称和双方卡组 |
| `LEAVE` | 离开方 → 对手 | 通知对方离开 |

实时对局中不使用房间数据库保存这些高频操作。

---

## 6. 游戏内同步

### 6.1 同步模型与职责边界

联机采用 **确定性输入帧 Lockstep**。同步对象是“每个逻辑帧双方确认的操作输入”，不是实体坐标、速度或完整战场快照。

```text
VibeHub Room
    ↓ 可靠、有序的消息传输
输入帧接收与校验
    ↓ localInputs[tick] + remoteInputs[tick]
双方确认到同一个 confirmedTick
    ↓
update(FIXED_DELTA) 推进同一个逻辑帧
    ↓
状态哈希校验与连接监控
```

逻辑帧固定为 30Hz。双方使用相同的随机种子、相同的确定性 `rand()` 序列和相同的输入执行顺序，各自计算完整战场状态。

联机子系统分为四层，职责不得混用：

1. **传输层**：负责 VibeHub Room 的创建、加入、消息发送、接收和对端事件；不推进游戏逻辑，不修改 `game`。
2. **输入确认层**：负责本地输入入帧、远端输入校验、去重、排序和按 tick 保存；不直接操作实体或扣除资源。
3. **模拟推进层**：只在双方输入帧均已确认时推进一个 `update(FIXED_DELTA)`；到期输入统一交给逻辑层执行。
4. **连接监控层**：负责 SYNC、超时、重连、状态哈希和断线回调；不得用心跳到达时间代替输入确认，也不得把普通网络抖动直接当作游戏指令。

`SYNC` 只用于进度观测、连接存活和哈希校验，不作为正常逻辑推进的唯一许可。逻辑暂停时渲染仍可继续，渲染累积时间不得因等待而无条件清零。

### 6.2 按 tick 的输入帧

输入帧是联机模拟的基本单位：

```js
{
    type: 'INPUT',
    tick: 120,
    seq: 7,
    commands: [
        {
            type: 'DEPLOY',
            team: 'player',
            cardId: 'xxx',
            x: 300,
            y: 250
        }
    ]
}
```

- `tick`：该输入帧归属的逻辑帧，不是消息到达帧。
- `seq`：发送方递增的输入帧编号，用于去重和诊断。
- `commands`：该 tick 内按固定规则排序的一组操作；没有操作时也发送空数组并视为已确认。
- `confirmedTick`：本端已同时取得双方输入帧的最大连续逻辑帧。
- 双方只能推进 `confirmedTick` 之后的下一个完整输入帧。
- 同一 tick 内的命令排序固定为阵营顺序，再按发送方 `seq` 排序；排序规则不能依赖网络到达顺序。

当前 `CMD + genTick + execTick` 队列是过渡实现。过渡期间必须保持以下兼容约束：`execTick` 由发送方生成 tick 加固定输入延迟计算；本地和远端操作都经同一执行入口；收到迟到或重复消息时不得重复执行。后续迁移完成后，`CMD` 不再承担推进许可语义。

### 6.3 输入帧生命周期

```text
本地操作
  → dispatchCommand()
  → 写入本地输入帧
  → 发送 INPUT
  → 等待远端同 tick 输入帧
  → confirmedTick 连续推进
  → update(FIXED_DELTA)
  → 执行该 tick 的全部 commands
```

每个输入帧必须经历以下状态：

- **收集**：允许本地操作写入当前可提交窗口；
- **提交**：输入帧内容冻结并发送，之后不得静默修改；
- **确认**：本地和远端输入帧均已收到并通过校验；
- **执行**：逻辑层在对应 tick 只执行一次；
- **回收**：执行后的输入和去重记录按窗口清理。

输入确认等待与连接断线必须分开处理：短暂等待只阻止进入缺少输入的 tick；只有超过真实时间超时且对端没有任何有效进展，才进入断线流程。

### 6.4 SYNC 与状态校验

双方继续使用游戏内 `SYNC` 消息，不使用 PING/PONG：

```js
{
    type: 'SYNC',
    tick,
    confirmedTick,
    lastInputTick,
    hashes: [[tick, hash]]
}
```

- `tick`：发送方当前模拟帧。
- `confirmedTick`：发送方已确认的连续输入帧。
- `lastInputTick`：发送方最近提交的输入帧，用于诊断输入延迟。
- `hashes`：最近状态哈希，用于发现确定性分叉。
- SYNC 由渲染帧按真实时间间隔发送，逻辑冻结期间也允许发送。
- 收到 SYNC 只更新连接监控和诊断数据，不直接推动本端模拟。

状态哈希按固定逻辑帧间隔计算，至少覆盖逻辑 tick、双方资源、实体稳定 ID、生命值、逻辑坐标、弹道和其他会影响确定性模拟的状态。哈希不用于恢复状态；发现不一致时只告警并记录 tick，处置策略另行定义。

### 6.5 输入安全检查

收到远端输入帧后必须检查：

- 消息结构、`tick`、`seq` 和 `commands` 类型合法；
- `tick` 位于允许的前后窗口内，过旧或过远的帧直接忽略；
- 输入帧编号未重复，重复消息不得再次入队；
- 每条命令的类型、阵营和字段合法，且阵营必须属于对手；
- 坐标、卡牌、技能和数量等数值通过既有业务校验；
- 未知字段和未知命令不得改变游戏状态。

校验失败的输入帧不得进入确认集合，也不得调用 `deploy()`、`castActiveSkill()` 等游戏执行函数。

---

## 6.6 重构约束与演进顺序

本节是同步架构的长期边界，不是开发任务清单：

- `dispatchCommand()` 是本地玩家、AI 和联机输入的统一入口；
- `network.js` 只管理会话、输入帧和网络状态，不直接编写战斗规则；
- `update.js` 是逻辑帧推进和到期输入执行的唯一权责文件；
- `main.js` 只负责固定步长循环、渲染循环和调用同步门控，不保存网络协议细节；
- `render.js` 不读取网络对象，也不修改 `game`；
- 第一阶段允许保留 `CMD` 兼容层，先分离 SYNC 与推进门控，再迁移为 `INPUT`；
- 在输入帧模型稳定并通过延迟、乱序、重复、短时断线测试前，不引入回滚或状态快照恢复。

---

## 7. 阵营与操作限制

- Host 操作蓝方 `player`，Client 操作红方 `ai`。
- 联机时玩家只能操作自己的阵营。
- 联机时跳过本地 AI 决策。
- 开局按 `NET_ROLE` 渲染己方卡组：Client 渲染到上方 `topCardPanel`，Host 渲染到下方 `cardPanel`。
- 联机时只显示己方卡牌面板；对方区域仅保留圣水条，其卡牌信息不对己方公开。
- 烟引卡暂时禁用，因为它的两段式操作还没有完成联机指令化。
- 技能释放统一经过 `dispatchCommand()`，再进入联机指令队列。

---

## 8. 连接状态与断线

游戏通过 VibeHub 的 `room.onPeer()` 接收：

- `join`：对手加入；
- `connecting`：正在建立连接；
- `reconnecting`：SDK 正在自动重连；
- `relay`：VibeNet 中继状态变化；
- `leave`：对手离开；
- `error`：连接错误。

VibeHub SDK 自动负责：

- WebRTC 连接；
- VibeNet 中继切换；
- 断线重连；
- presence 状态。

游戏只显示连接状态；对局中不使用 PING/PONG，改用游戏内 SYNC 心跳（见 6.1，900ms 间隔，兼具对手进展确认与哈希分叉检测；握手阶段的 HELLO 重发除外，见第 4 节）。

对局中确认对手离开后，游戏返回主页并提示联机已断开。

---

## 9. 数据存储规则

| 数据 | 处理方式 |
|---|---|
| 实时部署和技能 | `room.send()`，只走 P2P |
| 实体坐标、速度、圣水、攻击状态 | 双方本地计算，不保存 |
| 玩家卡组和设置 | 当前仍使用本地存储；以后可迁移到 `vibe.save` |
| 房间公开信息 | `room.announce()` |
| 单局低频结果 | 以后可选择写入 `room.data` |
| 全局排行榜 | 以后再设计使用 `vibe.global` |

禁止把坐标、每帧状态、操作流和实体快照写入数据存储。

---

## 10. 联机模式

当前提供两种联机入口：

- **双人联机（全卡）**：双方开局显示全部卡牌，不要求卡组。
- **双人联机（卡组）**：双方开局显示各自同步的卡组。

两种模式共用 VibeHub Room、大厅协议和 Lockstep 同步；烟引卡在两种联机模式中都暂时禁用。

---

## 11. 内容更新与平台同步方法

本节是日常更新内容、并把改动发布到 VibeHub 平台的标准方法。

### 11.1 更新流程总览

自动部署只发布仓库中已合并到 `main` 的内容；只修改本地源码目录不会自动同步到 GitHub 或 VibeHub。

```text
修改本地项目
    ↓
本地测试
    ↓
复制改动文件到发布仓库
    ↓
提交到特性分支并推送
    ↓
创建 PR，等待 build-and-deploy 检查通过
    ↓
squash 合并到 main
    ↓
GitHub Actions 自动构建
    ↓
VibeHub 自动执行 update
    ↓
检查 Actions 和 VibeHub 更新结果
```

### 11.2 VibeHub CLI 发布与维护约束

以下规则适用于首次发布、更新和 GitHub 自动部署；涉及 VibeHub CLI 的任务不得使用历史对话或本地旧副本替代官方最新规则。

#### CLI 与账号

- 每次使用 VibeHub CLI 前，先重新获取官方 Skill：`https://vibe.lumigrav.space/api/skill`，再按官方安装器检查或更新 CLI。
- 必须优先使用官方单文件 CLI；禁止要求用户安装 Node.js、Bun、npm、Rust/Cargo，也禁止自行编写 HTTP 上传、presign、multipart 或部署客户端。
- 先运行 `vibehub whoami` 复用本机登录凭证；只有明确报告未登录、凭证撤销或失效时才执行 `login`。网络或服务器故障不得反复登录。
- 项目级部署 Token 只能用于指定作品的自动部署，并使用 GitHub Secret `VIBEHUB_PROJECT_TOKEN`；不得索取或在工作流中使用账号级 Token。
- Git 凭据由 `gh auth git-credential` 提供；若 `git push` 报 `could not read Username`，先 `gh auth setup-git`，仍失败时手动配置 `git config credential.https://github.com.helper '!/usr/bin/gh auth git-credential'`（在发布仓库内执行）再 push。
- `gh auth login` 默认不含 `workflow` 权限：修改 `.github/workflows/*.yml` 前必须先 `gh auth refresh -h github.com -s workflow` 完成网页授权，否则 GitHub 会拒绝推送 workflow 文件。

#### 幂等与目标确认

- 所有发布操作必须先发现现状，再决定是否写入。重复执行同一任务不得创建第二个项目、版本、工作流、Ruleset、PR 或无意义 Token。
- 仓库根目录可用 `.vibehub/project.json` 保存非敏感项目映射，但不得写入 Token、内部 ID 或作品 ID；已有映射与账号真实项目冲突时停止并报告，不得覆盖或猜测。
- 每次发布前优先核对精确项目 slug：调用方提供的 slug → 项目映射 → 工作流中的 slug → `vibehub list`。必须使用项目 slug，不得把 `/play/` 或作品详情 URL 中的作品 ID、项目内部 ID、模糊名称或猜测值当作 slug。
- 已存在对应项目时只能执行 `update`；只有确认不存在对应项目时才执行一次 `deploy`。同一源码提交和构建产物已成功发布时，只回读结果，不重复创建版本。

#### 部署目录与作品资料

- 部署目录必须是实际静态输出根目录，顶层包含 `index.html`；不得把源码、`.git`、`node_modules`、构建缓存或 zip 包混入部署目录。
- 首次发布前分别询问两个独立选择：是否开启允许共创；首次发布成功后是否配置 GitHub 自动部署。不得替用户接受共创协议或代替选择。
- 分类和标签必须从 `vibehub metadata` 返回结果中选择，不得臆造。简介和版本说明使用 UTF-8 文件；封面和截图使用部署目录内的相对路径。
- CLI 报告编码、乱码或可疑文本时修复源文件后重试，不得使用绕过检查的参数掩盖真实乱码。

#### GitHub 自动部署与失败续办

- 用户选择自动部署或开放共创后，必须读取并执行官方 GitHub 自动化规范：`https://vibe.lumigrav.space/api/github-automation`。该规范是仓库验证、工作流、Ruleset、审批、Review、Token 和验收的唯一来源，禁止用手写 `gh api` 或历史规则替代。
- 开放共创前先运行 `vibehub collaboration-doctor --json`，只处理 doctor 明确报告的缺失依赖，再运行 `collaboration-setup` 让 CLI 接管仓库鉴权。
- GitHub Actions 与开放共创互不绑定；拒绝共创时仍可配置 CI，但不得传入共创参数。
- 发布、更新或 CI 中断/失败后，先检查项目、最新部署、仓库文件、PR、工作流和配置的真实状态，从第一个未完成步骤继续；不得盲目从头重放。自动部署只有首次 CI 验证完成后才算配置成功。

### 11.3 目录与账号环境

| 项目 | 内容 |
|---|---|
| 本地项目源码 | `/AstrBot/data/yuqingAI/未完成的ギャルゲ`（**本身不是 Git 仓库**） |
| 发布仓库 | `/tmp/yuqing-vibehub-release`（`/tmp` 重启可能丢失，丢失后从远端重新 clone 即可） |
| GitHub 远端 | `https://github.com/qingcheng7333-debug/tower-war.git` |
| VibeHub slug | `tower-war` |
| 试玩地址 | `https://vibe.lumigrav.space/play/eQKYXIIytzxZRX_Z8hnYN` |

工具环境：

- `gh` CLI 位于 `/root/.local/bin/gh`，已登录账号 `qingcheng7333-debug`，具备 `repo`、`workflow` 权限。
- Git 凭据由 `gh auth git-credential` 提供；`git push` 报 `could not read Username` 时按 11.2「CLI 与账号」处理。
- CI 配置 `.github/workflows/vibehub-deploy.yml` 只存在于发布仓库，本地源码目录没有 `.github`；修改 CI 只能在发布仓库进行（需 `workflow` 权限）。
- 源码目录与发布仓库是两个独立目录，**改动后必须手动复制文件过去，不会自动同步**。

### 11.4 分支与提交规则

- `main` 是正式发布分支，受仓库规则保护，**禁止直接 `git push origin main`**，必须先通过 `build-and-deploy` 状态检查才能合入。
- PR 只允许 **squash 合并**，不允许 merge commit。
- `vibehub/automation` 分支只用于官方自动部署配置，不用于日常开发。
- 日常更新必须走「特性分支 → PR → CI 通过 → squash 合并」流程。
- 只复制并 `git add` 本次改动的文件，不要无脑 `git add .`。
- 禁止提交 API Key、Token、`個人データ.js`、备份文件、缓存、`node_modules`（`.gitignore` 已拦截 `個人データ.js`、`*.bak` 等）。
- `?v=` 版本号平时不动，只在发布到平台时统一 bump（怎么改/为什么，见 11.5.1）。
- **squash 合并后不要在已合并分支上继续推新提交再开 PR**：main 上的 squash commit 与原分支内容相同但 hash 不同，GitHub 会把新 PR 标为 CONFLICTING。此时先 `git rebase origin/main`（git 自动跳过已应用的提交），再 `git push --force-with-lease`，PR 即可恢复可合并。

### 11.5 标准操作命令

```bash
cd /tmp/yuqing-vibehub-release
# 1. 先对齐远端 main（远端可能已被网页端 PR 合并过，本地 main 可能落后或分叉）
git fetch origin
git checkout main && git rebase origin/main
# 2. 只复制本次改动的文件，并本地语法校验
cp "<源码目录>/js/xxx.js" js/xxx.js
node --check js/xxx.js
# 3. 建特性分支、提交、推送
git checkout -b fix/xxx
git add js/xxx.js index.html   # 只 add 本次实际改动文件
git commit -m "描述改动"
git push -u origin fix/xxx
# 4. 创建 PR 并等待检查通过
/root/.local/bin/gh pr create --repo qingcheng7333-debug/tower-war \
    --base main --head fix/xxx --title "标题" --body "说明"
/root/.local/bin/gh pr checks <PR编号> --repo qingcheng7333-debug/tower-war
# 5. squash 合并，触发 main 部署
/root/.local/bin/gh pr merge <PR编号> --repo qingcheng7333-debug/tower-war --squash
# 6. 确认合并后 main 的部署工作流为 success
/root/.local/bin/gh run list --repo qingcheng7333-debug/tower-war --branch main --limit 3
```

#### 11.5.1 版本号策略（平时不动，只在平台发布时统一 bump）

- **`?v=` 是干什么的**：唯一作用是平台（VibeHub）CDN 缓存破坏——CDN/浏览器按完整 URL（含 `?v=`）缓存文件，版本号变了才会拉取新文件。
- **什么时候改**：**只在把游戏更新发布到平台时**（进入本章发布流程、复制进发布仓库之前），把 `index.html` 全部 18 处 `?v=`（1 处 CSS + 17 处 JS）统一 bump 成一个新值，一条命令搞定。**平时开发、本地游玩、改完代码自测：一律不动版本号**——本地不经 CDN，改动立刻生效，bump 没有任何意义。
- **为什么发布时必须 bump**：不 bump → 线上玩家拿到旧缓存 → 新旧代码混跑 → 联机两端 Lockstep 直接崩溃，且极难排查（"我明明改了怎么没效果"）。
- **禁止**删除 `?v=` 参数，也禁止只改部分文件：缓存按 URL 逐个记，漏改哪个，哪个就吃旧缓存。
- **统一 bump 用一条命令**（无需临时脚本，写回保持 UTF-8 无 BOM；把 `'v=旧'` 换成当前版本串、`'v=新'` 换成新版本串）：

```powershell
powershell -NoProfile -Command "[System.IO.File]::WriteAllText('index.html', [System.IO.File]::ReadAllText('index.html').Replace('v=旧','v=新'))"
```

- 版本号格式随意，唯一硬要求是**新值 ≠ 旧值**；惯例 `v=YYYYMMDD<序号>`（如 `20260901a13`），同日多次发布按序号递增，只增不减。
- bump 后自查（输出必须是 18，为 0 说明没生效）：

```powershell
powershell -NoProfile -Command "([regex]::Matches([System.IO.File]::ReadAllText('index.html'), 'v=新版本串')).Count"
```

### 11.6 CI 与部署机制

- **PR 阶段的 CI 只校验、不部署**：检查 `index.html`、`css/style.css`、`js/vibehubAdapter.js`、`js/network.js` 存在，且所有 `js/*.js` 通过 `node --check`。提交前先本地跑一遍可提前拦截问题。
- **CI 目前不校验 `index.html` 的 HTML 结构**：坏 script 标签（如 `src="js/config.js?v=xxx<script src=...` 嵌套）不是 JS 语法错误，`node --check` 拦不住，曾导致 CI 全绿但线上全白。改 `index.html` 后提交前用 python3 `html.parser` 检查 script/link 标签闭合；后续 CI 也应加入该检查。
- **真正部署发生在合并后**：PR 合并到 `main` 触发 `push` 事件的工作流，安装官方 CLI 并使用项目级 `VIBEHUB_PROJECT_TOKEN` 执行 `vibehub update --slug tower-war`。合并后要单独确认这次运行的结果。
- 不要手动复制或公开项目 Token，也不要因等待时间较长而重复推送同一提交。
- 部署成功后线上页面可能存在 CDN 缓存；应以 GitHub Actions 运行结果为准。部署后抓取线上 `https://vibeapps.lumigrav.space/tower-war/_releases/<版本目录>/index.html`，核对 script/link 标签完整、`?v=` 版本号与本次一致，并抽查 JS 返回 200；再用无痕/无缓存窗口实测。

### 11.7 发布失败时的处理

- **构建失败**：查看 Actions 日志，修复代码后重新提交新的 commit。
- **VibeHub 更新失败**：保留失败日志，不要自行轮换 Token；先确认 Secret 和项目 slug，再重试官方流程。
- **页面仍显示旧版本**：等待 GitHub Actions 和 VibeHub 状态同步，并用无缓存刷新重新打开页面。
- **需要撤销一次更新**：使用 Git 回退到已知正常提交，再推送一个新的 `main` 提交；不要删除历史提交或强制覆盖远端。

### 11.8 联机改动的额外测试要求

- 修改卡牌/游戏数值时优先改 `js/config.js`，不要在 `render.js`、`update.js` 中复制数值，避免显示值与实际逻辑不一致。
- 联机协议或状态同步有改动时，除单机测试外，还要进行双浏览器联机回归测试（建房/加入/准备/开局/部署/技能/胜负/断线重连）。

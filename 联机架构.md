# 🔗 联机架构（VibeHub 双人联机·全卡 / 卡组模式）

> 定位：记录游戏联机系统的架构、流程、同步规则和当前平台接入状态。
> 本文档参考 `基础框架.md`、`联机准备/llms.txt`、`联机准备/skill.json` 及当前项目实现。
> 当前版本：VibeHub 适配版 v11.65（2026-08-27）。
> 开发进度见 `联机开发进度.md`。

---

## 1. 总体架构

**VibeHub SDK v3 Room（WebRTC P2P / VibeNet 中继）+ Lockstep 确定性同步（延迟缓冲）**

```text
┌──────────────┐       VibeHub Room        ┌──────────────┐
│  Host（房主） │ ◄── room.send() 消息 ──► │ Client（加入）│
│  蓝方 player  │   WebRTC / VibeNet       │  红方 ai     │
└──────────────┘                          └──────────────┘
```

- Host 是蓝方 `player`，Client 是红方 `ai`。
- 双方各自运行一份完整游戏逻辑，不把实时战场状态写入服务器。
- 双方只通过 VibeHub Room 交换大厅消息和游戏操作指令。
- VibeHub SDK 负责 WebRTC 连接、VibeNet 中继、连接状态和自动重连。
- 游戏使用固定 30Hz 逻辑帧、同步随机种子和确定性 `rand()`。
- 当前同步模型为 **lockstep**：适合本游戏严格的双人对战和确定性逻辑。

### VibeHub 平台约束

- 必须使用 `window.VibeHub` SDK。
- SDK 使用稳定版 v3，并通过绝对地址加载：
  `https://vibe.lumigrav.space/sdk/v3/vibehub.js`
- 禁止自建后端、数据库、WebSocket 和实时状态轮询。
- 实时操作通过 `room.send()` 进行 P2P 传输。
- `room.data`、`vibe.save` 和 `vibe.global` 不用于保存高频战斗状态。
- 公开房间发现使用 `vibe.rooms.list()`。

---

## 2. 文件分工

| 文件 | 职责 |
|---|---|
| `js/vibehubAdapter.js` | 初始化 VibeHub SDK、识别作品 slug、处理登录/退出和账号状态 |
| `js/network.js` | VibeHub Room 创建/加入/离开、大厅消息、游戏指令、Lockstep 队列、连接状态 |
| `js/main.js` | 联机入口、大厅 UI、登录检查、房间列表、开始对局、断线回主页 |
| `js/ui.js` | 阵营操作限制、卡牌面板、技能指令、联机禁用烟引卡 |
| `js/update.js` | 固定 30Hz 游戏推进、执行到期联机指令、在线模式跳过 AI |
| `js/gameState.js` | 游戏状态和随机种子重置；不保存网络连接对象 |
| `index.html` | 加载 VibeHub SDK 和游戏脚本，提供登录区及联机大厅 |
| `css/style.css` | VibeHub 账号区、联机大厅和连接状态样式 |

---

## 3. 平台初始化与登录

1. 游戏在 VibeHub 作品地址中从路径取得项目 slug。
2. 通过 `VibeHub.init({ work: slug })` 初始化 SDK。
3. VibeHub 作品页面显示登录、退出和当前账号状态。
4. 单机模式不要求登录；联机模式要求登录。
5. 登录凭证只由 SDK 保存在当前页面内存中，不写入 `localStorage` 或 `sessionStorage`。
6. 账号退出或授权失效时，游戏停止继续使用当前联机房间，并返回登录提示。

---

## 4. 房间与大厅流程

```text
未进入房间
  ├─ 房主：生成房间号 → VibeHub join → announce 公共房间
  └─ 加入者：输入房间号或查看公开房间 → VibeHub join
          ↓
      HELLO / JOIN_ACK
          ↓
      双方准备
          ↓
      房主发送 GAME_START
          ↓
        进入对局
```

- 房间使用 VibeHub `room.join(roomId, { topology: 'mesh' })`。
- 房主使用 `room.announce()` 发布低频房间信息。
- 公开房间列表使用 `vibe.rooms.list()`，只用于发现房间。
- 当前房间人数上限为 2 人。
- 房主关闭或玩家离开时调用 `room.leave()`。
- VibeHub SDK 负责连接重连和中继切换，游戏不再实现自定义心跳。

### 房间状态

```text
idle → hosting → 对手加入 → 双方准备 → in_game
idle → joined  → 收到房主确认 → 双方准备 → in_game
```

对应状态值：

```text
idle / hosting / joined / in_game
```

---

## 5. 大厅消息

所有大厅消息通过 `room.send()` 传输。

| 消息 | 方向 | 作用 |
|---|---|---|
| `HELLO` | Client → Host | 发送昵称和卡组 |
| `JOIN_ACK` | Host → Client | 确认加入并同步双方卡组 |
| `LOBBY_READY` | 双向 | 同步准备状态 |
| `GAME_START` | Host → Client | 同步随机种子、昵称和双方卡组 |
| `LEAVE` | 离开方 → 对手 | 通知对方离开 |

实时对局中不使用房间数据库保存这些高频操作。

---

## 6. 游戏内同步

### 6.1 同步方式

游戏采用 Lockstep 延迟缓冲：

```text
同一种子 + 相同操作顺序 + 相同确定性逻辑
                    ↓
              双方自行推进战场
```

逻辑帧固定为 30Hz。双方不发送每帧坐标、速度或完整实体状态。

### 6.2 操作指令

目前支持：

```js
DEPLOY  // 部署卡牌
SKILL   // 释放技能
```

消息结构：

```js
{
    type: 'CMD',
    genTick,
    seq,
    cmd: {
        type,
        team,
        ...payload
    }
}
```

- `genTick`：指令产生时的逻辑帧。
- `seq`：发送方递增的指令编号。
- `execTick = genTick + 10`：约 333 毫秒延迟缓冲。
- 双方按照 `execTick`、阵营顺序、`seq` 排序执行。
- 重要操作使用 VibeHub 可靠、有序的 `room.send()`，不使用允许丢包的实时通道。

### 6.3 指令安全检查

收到远程指令后检查：

- 指令格式；
- 指令编号是否重复；
- 产生帧是否合理；
- 阵营是否属于对手；
- 指令类型是否允许。

不合格的远程指令直接忽略，不进入游戏执行队列。

---

## 7. 阵营与操作限制

- Host 操作蓝方 `player`。
- Client 操作红方 `ai`。
- 联机时玩家只能操作自己的阵营。
- 联机时跳过本地 AI 决策。
- 双方分别显示自己的卡组和对方卡组。
- 烟引卡暂时禁用，因为它的两段式操作还没有完成联机指令化。
- 技能释放统一经过 `dispatchCommand()`，再进入联机指令队列。

---

## 8. 连接状态与断线

游戏通过 VibeHub 的 `room.onPeer()` 接收：

- `join`：对手加入；
- `connecting`：正在建立连接；
- `reconnecting`：SDK 正在自动重连；
- `relay`：VibeNet 中继状态变化；
- `leave`：对手离开；
- `error`：连接错误。

VibeHub SDK 自动负责：

- WebRTC 连接；
- VibeNet 中继切换；
- 断线重连；
- presence 状态。

游戏只显示连接状态，不自行发送 PING/PONG，不使用自定义超时心跳。

对局中确认对手离开后，游戏返回主页并提示联机已断开。

---

## 9. 数据存储规则

| 数据 | 处理方式 |
|---|---|
| 实时部署和技能 | `room.send()`，只走 P2P |
| 实体坐标、速度、圣水、攻击状态 | 双方本地计算，不保存 |
| 玩家卡组和设置 | 当前仍使用本地存储；以后可迁移到 `vibe.save` |
| 房间公开信息 | `room.announce()` |
| 单局低频结果 | 以后可选择写入 `room.data` |
| 全局排行榜 | 以后再设计使用 `vibe.global` |

禁止把坐标、每帧状态、操作流和实体快照写入数据存储。

---

## 10. 联机模式

当前提供两种联机入口：

- **双人联机（全卡）**：双方开局显示全部卡牌，不要求卡组。
- **双人联机（卡组）**：双方开局显示各自同步的卡组。

两种模式共用 VibeHub Room、大厅协议和 Lockstep 同步；烟引卡在两种联机模式中都暂时禁用。

---

## 11. 当前实现状态

### 已完成

- VibeHub SDK v3 稳定版接入；
- VibeHub 登录、退出和账号状态适配；
- VibeHub Room 创建和加入接口适配；
- VibeHub `room.send()` 消息传输；
- VibeHub `room.onPeer()` 连接状态适配；
- 公开房间查询；
- Lockstep 延迟缓冲保留；
- `DEPLOY` / `SKILL` 指令保留；
- PeerJS 已从生产代码移除；
- 自定义 PING/PONG 心跳已移除；
- 单机模式继续保留；
- 已允许共创；公开 GitHub 仓库已完成 VibeHub 共创仓库鉴权。

### 尚未完成

- 双人联机（全卡 / 卡组）代码入口已完成，已发布到 VibeHub，真实联机回归测试仍待执行；
- 已正式发布到 VibeHub；
- 正式项目 slug 已确认为 `tower-war`；
- 尚未在真实 VibeHub 作品地址进行双浏览器联机测试；
- 试玩地址：`https://vibe.lumigrav.space/play/eQKYXIIytzxZRX_Z8hnYN`；
- 尚未验证真实 VibeNet 中继和自动重连；
- GitHub 私有仓库自动部署已配置，配置 PR #1 待合并；
- 烟引卡联机仍未开放；
- 已配置 `.vibehub/project.json`；自动部署配置 PR 合并后生效。

---

## 12. 测试计划

### 本地静态检查

- 全部 JavaScript 语法检查；
- VibeHub SDK 加载顺序检查；
- PeerJS、WebSocket、自定义心跳残留检查；
- 单机模式相关接口检查。

### VibeHub 实际测试

发布后使用两个浏览器窗口测试：

1. 两个账号分别登录 VibeHub；
2. 一方创建房间；
3. 另一方通过列表或房间号加入；
4. 双方确认昵称和卡组；
5. 双方准备；
6. 房主开始对局；
7. 测试部署、技能和胜负；
8. 测试离开房间、断线、自动重连和 VibeNet 中继。

---

## 13. 发布前后续工作

1. 清理发布目录，排除 API Key、个人数据、备份和测试文件。
2. 按官方 CLI Skill 安装或更新 VibeHub CLI。
3. 检查账号、已有项目和精确 slug。
4. 准备作品标题、简介、分类、标签、封面和截图。
5. GitHub 自动部署已配置；后续更新按第 14 节执行。
6. 使用官方 CLI 发布，不手写上传 API。
7. 在真实 VibeHub 地址进行联机测试。
8. 测试通过后，再考虑云存档、排行榜、烟引卡和全卡联机。

当前发布选择：

```text
允许共创：允许（仓库鉴权已完成）
GitHub 自动部署：已配置并通过首次部署验收
```


---

## 14. 后续内容更新与同步方法

### 14.1 推荐更新流程

游戏内容、卡牌数值和配置修改后，统一按以下顺序同步：

```text
修改本地项目
    ↓
本地测试
    ↓
同步到 GitHub 仓库的 main 分支
    ↓
GitHub Actions 自动构建
    ↓
VibeHub 自动执行 update
    ↓
检查 Actions 和 VibeHub 更新结果
```

自动部署只发布仓库中已提交并推送到 `main` 的内容；只修改本地文件不会自动同步到 GitHub 或 VibeHub。

### 14.2 调整 config.js 数值

具体卡牌和游戏数值应优先修改：

```text
js/config.js
```

例如把某张卡的攻击力从 `100` 改为 `120` 后，先在本地浏览器测试部署、战斗、AI 和联机相关行为。不要在 `render.js`、`update.js` 等文件中复制一份数值，否则可能造成显示值与实际逻辑不一致。

### 14.3 使用 Git 同步

在 GitHub 仓库的本地目录执行：

```bash
git status
git diff
# 确认改动内容后
git add js/config.js
git commit -m "调整卡牌数值"
git push origin main
```

修改多个确定文件时，可以明确列出文件：

```bash
git add js/config.js js/entities.js
git commit -m "调整单位数值与部署逻辑"
git push origin main
```

不建议未经检查直接使用 `git add .`。禁止提交 API Key、Token、`個人データ.js`、备份文件、缓存、`node_modules` 和 `整活` 目录。

### 14.4 自动部署检查

推送后前往 GitHub 仓库的 **Actions** 页面，确认 `Build and deploy to VibeHub` 工作流：

1. 构建步骤通过；
2. `Publish built files` 步骤通过；
3. 对应提交的运行结果为成功。

工作流成功后，VibeHub 会使用项目级 `VIBEHUB_PROJECT_TOKEN` 执行：

```text
vibehub update --slug tower-war
```

不要手动复制或公开项目 Token，也不要因为等待时间较长而重复推送同一提交。

### 14.5 发布失败时的处理

- 构建失败：先查看 Actions 日志，修复代码后重新提交新的 commit。
- VibeHub 更新失败：保留失败日志，不要自行轮换 Token；先确认 Secret 和项目 slug，再重试官方流程。
- 页面仍显示旧版本：等待 GitHub Actions 和 VibeHub 状态同步，并用无缓存刷新重新打开页面。
- 需要撤销一次更新：使用 Git 回退到已知正常提交，再推送一个新的 `main` 提交；不要删除历史提交或强制覆盖远端。

### 14.6 重要分支规则

- `main` 是正式发布分支。
- `vibehub/automation` 只用于官方自动部署配置 PR，不用于日常开发。
- 日常修改完成后，必须通过 GitHub Actions 成功后才视为 VibeHub 更新完成。
- 联机协议或状态同步有改动时，除单机测试外，还要进行双浏览器联机回归测试。
